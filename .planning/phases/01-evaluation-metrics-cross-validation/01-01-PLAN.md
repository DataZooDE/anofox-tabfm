---
phase: 01-evaluation-metrics-cross-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - src/include/tabfm_registration.hpp
  - src/anofox_tabfm_extension.cpp
  - src/include/anofox_function_alias.hpp
  - src/tabfm_metrics_classification.cpp
  - src/tabfm_metrics_regression.cpp
  - src/tabfm_crossval.cpp
  - src/include/tabfm_metrics_classification.hpp
  - src/include/tabfm_metrics_regression.hpp
  - src/include/tabfm_crossval.hpp
  - test/cpp/test_tabfm_metrics.cpp
  - test/cpp/test_tabfm_metrics_regression.cpp
  - test/cpp/test_tabfm_crossval.cpp
  - test/sql/tabfm_metrics_classification.test
  - tools/golden/generate_metric_fixtures.py
autonomous: true
requirements: [CMET-01]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SELECT tabfm_accuracy(actual, predicted) FROM t returns correct/total as DOUBLE, matching a scikit-learn golden value (CMET-01)"
    - "anofox_tabfm_accuracy and its short alias tabfm_accuracy are both registered and callable"
    - "accuracy skips rows where actual or predicted is NULL (SQL aggregate semantics)"
    - "make debug builds the cpu-flavor extension with the three new modules linked (empty Register* bodies compile)"
  artifacts:
    - CMakeLists.txt
    - src/include/tabfm_registration.hpp
    - src/anofox_tabfm_extension.cpp
    - src/include/anofox_function_alias.hpp
    - src/tabfm_metrics_classification.cpp
    - src/tabfm_metrics_regression.cpp
    - src/tabfm_crossval.cpp
    - src/include/tabfm_metrics_classification.hpp
    - src/include/tabfm_metrics_regression.hpp
    - src/include/tabfm_crossval.hpp
    - test/cpp/test_tabfm_metrics.cpp
    - test/cpp/test_tabfm_metrics_regression.cpp
    - test/cpp/test_tabfm_crossval.cpp
    - test/sql/tabfm_metrics_classification.test
    - tools/golden/generate_metric_fixtures.py
  key_links:
    - "anofox_tabfm_extension.cpp Load() calls RegisterClassificationMetrics/RegisterRegressionMetrics/RegisterCrossValidateMacros"
    - "RegisterAggregateFunctionSetWithAlias in anofox_function_alias.hpp registers primary anofox_tabfm_* + alias tabfm_*"
    - "CMakeLists EXTENSION_SOURCES lists the three new .cpp; TABFM_CPP_TEST_SOURCES lists all three metric test TUs (so Wave-2 plans never touch CMakeLists)"
---

<objective>
As a data analyst, I want to compute classification accuracy directly in SQL over my
(actual, predicted) columns, so that I can measure a model's correctness without leaving DuckDB.

This is the Wave-0 tracer plan. It does two jobs at once: (1) wires ALL scaffold-owned shared
files once so the three parallel Wave-2 feature plans never collide on them, and (2) ships ONE
metric — `tabfm_accuracy` (CMET-01) — fully end-to-end (aggregate + full-name/short-alias
registration + telemetry-at-bind + a scikit-learn golden sqllogictest) to prove the entire
registration/alias/telemetry/test pipeline before any expansion.

Purpose: prove the architecture on the agent's best early-context tokens; unblock parallel plans.
Output: three empty-body-registered modules linked into the build, a reusable aggregate-alias
helper, a shared sklearn golden-fixture generator, and a working, golden-verified accuracy metric.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-evaluation-metrics-cross-validation/01-CONTEXT.md
@.planning/phases/01-evaluation-metrics-cross-validation/01-RESEARCH.md
@.planning/phases/01-evaluation-metrics-cross-validation/01-PATTERNS.md

# Canonical analogs — read the exact line ranges named in 01-PATTERNS.md
@src/tabfm_predict_agg.cpp
@src/include/anofox_function_alias.hpp
@src/include/tabfm_registration.hpp
@src/anofox_tabfm_extension.cpp
@CMakeLists.txt
@test/sql/tabfm_classify.test
@test/cpp/test_tabfm_scaffold.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coordinated scaffold wiring for all three modules (one batch)</name>
  <files>CMakeLists.txt, src/include/tabfm_registration.hpp, src/anofox_tabfm_extension.cpp, src/include/anofox_function_alias.hpp, src/include/tabfm_metrics_classification.hpp, src/include/tabfm_metrics_regression.hpp, src/include/tabfm_crossval.hpp, src/tabfm_metrics_classification.cpp, src/tabfm_metrics_regression.cpp, src/tabfm_crossval.cpp, test/cpp/test_tabfm_metrics.cpp</files>
  <precondition>Working tree is on the pinned DuckDB v1.5.4 submodule and `make debug` currently succeeds on this checkout.</precondition>
  <action>
    This task owns ALL scaffold-owned shared-file edits for the whole phase (CLAUDE.md rule #2), done as one coordinated batch so the parallel feature plans (02/03/04) never touch these files again.
    1. Add the aggregate-alias helper `RegisterAggregateFunctionSetWithAlias` to `anofox_function_alias.hpp`, mirroring the existing `RegisterScalarFunctionSetWithAlias` exactly (copy-then-rename every overload so all metadata survives; set `alias_info.alias_of = func_set.name`). Add the include `duckdb/parser/parsed_data/create_aggregate_function_info.hpp` to that header. See 01-PATTERNS.md "anofox_function_alias.hpp (edit)".
    2. Create the three public headers `src/include/tabfm_metrics_classification.hpp`, `tabfm_metrics_regression.hpp`, `tabfm_crossval.hpp`, each declaring one register function (`RegisterClassificationMetrics`, `RegisterRegressionMetrics`, `RegisterCrossValidateMacros`) taking `ExtensionLoader &loader`, wrapped in `namespace duckdb { namespace anofox { ... } }`, following the tabfm_registration.hpp header pattern.
    3. Declare the three register functions in `src/include/tabfm_registration.hpp` after the existing declarations.
    4. Create `src/tabfm_metrics_regression.cpp` and `src/tabfm_crossval.cpp` with a namespaced empty-body `RegisterRegressionMetrics`/`RegisterCrossValidateMacros` (bodies filled by plans 03/04). Create `src/tabfm_metrics_classification.cpp` with the real accuracy implementation (Task 2 fills its body; leave a compiling `RegisterClassificationMetrics` stub here that Task 2 extends). Each file uses the WS/spec header comment convention and the `namespace duckdb { namespace anofox {` layout.
    5. Call the three register functions in `src/anofox_tabfm_extension.cpp` Load(), after the existing `RegisterPredictMacros(loader)` call.
    6. Add the three new `.cpp` to `EXTENSION_SOURCES` (after src/tabfm_macros.cpp) and add ALL THREE metric test TUs — `test/cpp/test_tabfm_metrics.cpp` (classification), `test/cpp/test_tabfm_metrics_regression.cpp` (regression), `test/cpp/test_tabfm_crossval.cpp` (CV) — to `TABFM_CPP_TEST_SOURCES` now, so Wave-2 plans 03/04 never need to touch CMakeLists again (avoids the shared-file collision noted in 01-RESEARCH.md Pitfall 6). Create each of the three test TUs as a minimal Catch2 TU (`#include "catch.hpp"`, `#include "duckdb.hpp"`) with one placeholder passing test so the source list compiles; plans 02/03/04 add real cases.
    Use full names `anofox_tabfm_*` + short alias `tabfm_*` per CLAUDE.md rule #4. Do not implement any metric math in this task except what Task 2 covers.
  </action>
  <verify>
    <automated>make debug</automated>
    <fails_when>non-zero exit, or a compiler/linker error mentioning any of the new files or an undefined RegisterClassificationMetrics/RegisterRegressionMetrics/RegisterCrossValidateMacros symbol</fails_when>
  </verify>
  <done>make debug produces build/debug/anofox_tabfm_loadable_extension.so with all three modules linked and the three Register* functions called from Load(); empty-body register functions compile.</done>
  <reversibility rating="reversible">Scaffold edits are additive; revertible by removing the new source-list entries and declarations.</reversibility>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end tracer — tabfm_accuracy fully wired with a golden test (CMET-01)</name>
  <files>tools/golden/generate_metric_fixtures.py, test/sql/tabfm_metrics_classification.test, src/tabfm_metrics_classification.cpp</files>
  <behavior>
    - Golden: on a 3-row 3-class fixture where 2 of 3 predictions are correct, tabfm_accuracy = 0.666667 (round to 6 dp), matching scikit-learn accuracy_score.
    - NULL skip: a row with NULL actual or NULL predicted is excluded from both numerator and denominator.
    - Empty/all-NULL input: tabfm_accuracy returns NULL (state.total == 0 -> FlatVector::SetNull).
    - Alias parity: tabfm_accuracy and anofox_tabfm_accuracy return the identical value on the same input.
  </behavior>
  <action>
    RED first, then GREEN. This is the vertical slice that proves the whole pipeline.
    1. Write `tools/golden/generate_metric_fixtures.py` — a small uv/stdlib+sklearn script that prints golden metric values (start with accuracy_score on the 3-class fixture) so committed golden values in the .test file are traceable to sklearn. Document in a comment that it is a fixture-generation aid, not a build artifact, and is not listed in CMakeLists. Committed hard-coded sklearn values in the .test file are the source of truth (note this choice in the SUMMARY).
    2. Write the FAILING `test/sql/tabfm_metrics_classification.test` (RED): header per 01-PATTERNS.md, `require anofox_tabfm` + `LOAD anofox_tabfm`, a 3-class VALUES fixture, and `query I SELECT round(tabfm_accuracy(actual, predicted), 6) FROM preds ----` expecting `0.666667`; a NULL-skip case; an all-NULL/empty case returning NULL; and an alias-parity check that anofox_tabfm_accuracy equals tabfm_accuracy. Run the single test and confirm it FAILS before implementing.
    3. Implement (GREEN) the accuracy aggregate in `src/tabfm_metrics_classification.cpp`: `AccuracyState {int64_t correct; int64_t total;}` and the state-size/init/update/combine/finalize triplet using the UnifiedVectorFormat NULL-skip pattern (01-PATTERNS.md "NULL skip in Update"). Update skips rows where actual OR predicted is NULL via the validity mask; a matching pair increments correct, every valid pair increments total. Finalize writes correct/total as DOUBLE, or SetNull when total==0. Register `anofox_tabfm_accuracy` ({ANY, ANY} -> DOUBLE) with a Bind callback that calls `PostHogTelemetry::Instance().CaptureFunctionExecution("tabfm_accuracy")` exactly once (bind, never per-row, per CLAUDE.md rule #3), and register the `tabfm_accuracy` alias via the new `RegisterAggregateFunctionSetWithAlias` helper.
    4. Add one Catch2 case to `test/cpp/test_tabfm_metrics.cpp` exercising accuracy through a `duckdb::Connection` (load extension, run the aggregate, assert the value) — mirroring test_tabfm_scaffold.cpp.
    Run the single sqllogictest and confirm it now PASSES.
  </action>
  <verify>
    <automated>make debug && ./build/debug/test/unittest test/sql/tabfm_metrics_classification.test</automated>
    <fails_when>non-zero exit, or "FAILED" in the output, or the accuracy query returns a value other than 0.666667, or the empty-input case is non-NULL</fails_when>
    <human-check>Confirm the tracer slice is proven end-to-end before Wave-2 expansion: both tabfm_accuracy and anofox_tabfm_accuracy resolve and agree, the three scaffold register functions are wired in Load() and the build links cleanly, and the aggregate-alias helper is in use.</human-check>
  </verify>
  <done>tabfm_accuracy and anofox_tabfm_accuracy both return 0.666667 on the golden fixture, skip NULL rows, return NULL on empty input, and match each other; the sqllogictest passes end-to-end from a fresh build.</done>
  <reversibility rating="reversible">Pure additive metric; revertible by removing the accuracy block and its test.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL query → aggregate Update | User column values (untrusted) cross into C++ accumulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Denial of Service | AccuracyFinalize division | low | mitigate | Guard total==0 → SetNull; never divide by zero (V5 input validation) |
| T-01-02 | Tampering | int64 accumulator overflow on very large inputs | low | accept | int64_t correct/total covers realistic row counts; residual risk accepted |
| T-01-03 | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase; RESEARCH.md Package Legitimacy Audit records zero packages |

No high-severity threats. Metric aggregates operate on plain columns with no external input surface. The material phase threat (SQL-identifier injection) lives in the CV plan (01-04), not here.
</threat_model>

<verification>
- `make debug` links all three new modules (empty bodies compile).
- `./build/debug/test/unittest test/sql/tabfm_metrics_classification.test` passes.
- `tabfm_accuracy` == `anofox_tabfm_accuracy` on the golden fixture (0.666667).
- NULL rows skipped; empty input → NULL.
- Telemetry called once at bind (grep the source: exactly one CaptureFunctionExecution per bind body).
</verification>

<success_criteria>
Scaffold wired once for the whole phase; `tabfm_accuracy` (CMET-01) works end-to-end and matches scikit-learn on a committed golden fixture; parallel feature plans are unblocked.
</success_criteria>

<output>
Create `.planning/phases/01-evaluation-metrics-cross-validation/01-01-SUMMARY.md` when done.
</output>
