---
phase: 01-evaluation-metrics-cross-validation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/tabfm_metrics_regression.cpp
  - test/sql/tabfm_metrics_regression.test
  - test/cpp/test_tabfm_metrics_regression.cpp
  - tools/golden/generate_metric_fixtures.py
autonomous: true
requirements: [RMET-01, RMET-02, RMET-03, RMET-04]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "tabfm_rmse(actual, predicted) matches sklearn root_mean_squared_error on a golden fixture (RMET-01)"
    - "tabfm_mae(actual, predicted) matches sklearn mean_absolute_error (RMET-02)"
    - "tabfm_r2(actual, predicted) matches sklearn r2_score AND returns 1.0/0.0 on a constant-target fixture per the sklearn convention (RMET-03)"
    - "tabfm_mape(actual, predicted) skips zero-actual rows and matches sklearn on the non-zero rows; tabfm_medae returns the median absolute error (RMET-04)"
  artifacts:
    - src/tabfm_metrics_regression.cpp
    - test/sql/tabfm_metrics_regression.test
  key_links:
    - "each aggregate registered via RegisterAggregateFunctionSetWithAlias (anofox_tabfm_* + tabfm_*)"
    - "medae heap-owning vector<double> state uses StateInit/StateDestroy"
    - "r2 finalize guards SS_tot==0 before division"
---

<objective>
As a data analyst, I want to compute regression quality metrics — RMSE, MAE, R² (constant-target
safe), MAPE (zero-actual safe), and median absolute error — directly in SQL, so that I can measure
regression prediction quality on my own tables without crashes on edge cases.

Purpose: deliver RMET-01..04, expanding the empty regression module seeded by plan 01-01 (this
plan owns src/tabfm_metrics_regression.cpp and its own test files — no file collision with the
classification or CV plans, so it runs in parallel in Wave 2).
Output: five regression metrics, each golden-verified against scikit-learn, with documented,
non-crashing edge-case behavior.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-evaluation-metrics-cross-validation/01-CONTEXT.md
@.planning/phases/01-evaluation-metrics-cross-validation/01-RESEARCH.md
@.planning/phases/01-evaluation-metrics-cross-validation/01-PATTERNS.md

@src/tabfm_predict_agg.cpp
@src/include/anofox_function_alias.hpp
@CMakeLists.txt
@.planning/phases/01-evaluation-metrics-cross-validation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RMSE and MAE aggregates (RMET-01, RMET-02)</name>
  <files>tools/golden/generate_metric_fixtures.py, test/sql/tabfm_metrics_regression.test, src/tabfm_metrics_regression.cpp, test/cpp/test_tabfm_metrics_regression.cpp</files>
  <precondition>Plan 01-01 has landed: src/tabfm_metrics_regression.cpp exists with an empty RegisterRegressionMetrics body, RegisterAggregateFunctionSetWithAlias is available in anofox_function_alias.hpp, and test/cpp/test_tabfm_metrics_regression.cpp is already registered in TABFM_CPP_TEST_SOURCES (no CMakeLists edit needed here).</precondition>
  <behavior>
    - Golden RMSE: tabfm_rmse(actual, predicted) matches sklearn root_mean_squared_error on the fixture (tolerance 1e-6).
    - Golden MAE: tabfm_mae(actual, predicted) matches sklearn mean_absolute_error.
    - NULL rows skipped from both numerator and denominator.
    - Empty/all-NULL input → NULL.
  </behavior>
  <action>
    RED first. Create `test/sql/tabfm_metrics_regression.test` (header per 01-PATTERNS.md, require/LOAD anofox_tabfm, a numeric fixture) with FAILING golden queries for rmse and mae; add the sklearn golden values to `tools/golden/generate_metric_fixtures.py`. The Catch2 TU `test/cpp/test_tabfm_metrics_regression.cpp` is already registered in CMakeLists by plan 01-01 — fill its body here, do NOT touch CMakeLists. Confirm RED.
    Implement RMSE state `{double sum_sq; int64 n}` (sum_sq += (predicted-actual)^2; finalize sqrt(sum_sq/n)) and MAE state `{double sum_abs; int64 n}` (finalize sum_abs/n), both with the UnifiedVectorFormat NULL-skip pattern and SetNull on n==0. Inputs typed DOUBLE (accept {DOUBLE, DOUBLE}; rely on DuckDB implicit numeric casts). Register anofox_tabfm_rmse/mae + tabfm_rmse/mae aliases via RegisterAggregateFunctionSetWithAlias; telemetry once per bind. Add a Catch2 case for rmse through a Connection in test_tabfm_metrics_regression.cpp.
  </action>
  <verify>
    <automated>make debug && ./build/debug/test/unittest test/sql/tabfm_metrics_regression.test</automated>
    <fails_when>non-zero exit, or "FAILED" in output, or rmse/mae disagree with sklearn beyond 1e-6, or empty input is non-NULL</fails_when>
  </verify>
  <done>tabfm_rmse and tabfm_mae match sklearn on the golden fixture, skip NULLs, and return NULL on empty input; both aliases resolve.</done>
  <reversibility rating="reversible">Additive metric blocks; no scaffold edits.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: R² with the constant-target edge case (RMET-03)</name>
  <files>tools/golden/generate_metric_fixtures.py, test/sql/tabfm_metrics_regression.test, src/tabfm_metrics_regression.cpp</files>
  <behavior>
    - Golden R²: tabfm_r2(actual, predicted) matches sklearn r2_score on the fixture (may be negative for a poor model).
    - Constant target, perfect prediction: all actual equal AND predicted == actual → R² = 1.0.
    - Constant target, imperfect prediction: all actual equal AND some predicted != actual → R² = 0.0 (never NaN/Inf).
  </behavior>
  <action>
    RED first. Add sklearn r2_score golden value on the fixture and TWO constant-target cases (perfect→1.0, imperfect→0.0) as FAILING queries. Confirm RED.
    Implement online state `{double sum_y; double sum_y2; double sum_res; int64 n}` (sum_res += (actual-predicted)^2). Finalize: y_bar=sum_y/n; SS_tot=sum_y2 - n*y_bar^2; SS_res=sum_res. Guard SS_tot==0 (use a tight tolerance, e.g. |SS_tot| < 1e-12): return 1.0 if SS_res==0 (within tolerance) else 0.0 — the documented sklearn convention (D: R² constant-target). Otherwise return 1 - SS_res/SS_tot (may be negative). Never divide by zero (Pitfall 3). Document the convention in the function description. Register anofox_tabfm_r2 + tabfm_r2; telemetry once per bind.
  </action>
  <verify>
    <automated>make debug && ./build/debug/test/unittest test/sql/tabfm_metrics_regression.test</automated>
    <fails_when>non-zero exit, or "FAILED" in output, or R² returns nan/inf on the constant-target cases, or the golden value disagrees with sklearn</fails_when>
  </verify>
  <done>tabfm_r2 matches sklearn on the general fixture and returns exactly 1.0 (perfect) or 0.0 (imperfect) on constant-target data with no NaN/Inf.</done>
  <reversibility rating="reversible">Additive metric block.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MAPE (zero-actual safe) and median absolute error (RMET-04)</name>
  <files>tools/golden/generate_metric_fixtures.py, test/sql/tabfm_metrics_regression.test, src/tabfm_metrics_regression.cpp</files>
  <behavior>
    - MAPE: tabfm_mape(actual, predicted) skips rows where actual == 0 and matches sklearn mean_absolute_percentage_error computed over the non-zero rows (document dimensionless-ratio-vs-percent choice; match whichever the golden value uses).
    - MAPE all-zero actuals: returns NULL (no rows contribute), no crash.
    - MedAE: tabfm_medae(actual, predicted) returns the median of |actual-predicted| matching sklearn median_absolute_error; even-N returns the mean of the two middle residuals.
  </behavior>
  <action>
    RED first. Add sklearn MAPE (over non-zero rows) and median_absolute_error golden values, plus an all-zero-actual MAPE case (→ NULL), as FAILING queries. Document in the .test comment whether MAPE is a dimensionless ratio or a percentage and match the golden value accordingly. Confirm RED.
    MAPE state `{double sum_ape; int64 n}`: for each valid row, if actual==0 skip (documented, Pitfall on MAPE); else sum_ape += |actual-predicted|/|actual|, n++; finalize sum_ape/n (SetNull if n==0). MedAE state is heap-owning `vector<double>` residuals → StateInit allocates, StateDestroy frees (01-PATTERNS.md StateDestroy pattern); Update pushes |actual-predicted|; Combine merges the vectors; Finalize sorts (std::sort) and returns the middle element (or mean of two middles for even N), SetNull if empty. Document MedAE's O(N) state growth. Register anofox_tabfm_mape/medae + tabfm_mape/medae aliases; telemetry once per bind.
  </action>
  <verify>
    <automated>make debug && ./build/debug/test/unittest test/sql/tabfm_metrics_regression.test</automated>
    <fails_when>non-zero exit, or "FAILED" in output, or MAPE crashes/divides by zero on a zero-actual row, or MAPE/MedAE disagree with sklearn beyond 1e-6</fails_when>
  </verify>
  <done>tabfm_mape skips zero-actual rows (returns NULL when all are zero) and matches sklearn on non-zero rows; tabfm_medae returns the correct median absolute error; neither crashes on edge cases.</done>
  <reversibility rating="reversible">Additive metric blocks.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL query → aggregate Update | Untrusted numeric actual/predicted column values enter C++ accumulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03-01 | Denial of Service | division by zero (R² constant target, MAPE zero actual, empty state) | low | mitigate | R² guards SS_tot==0; MAPE skips actual==0; all finalizers SetNull on empty (V5 input validation) |
| T-01-03-02 | Tampering | double accumulator precision loss on very large inputs | low | accept | 64-bit IEEE 754 sums have sufficient range for realistic datasets; residual accepted |
| T-01-03-03 | Denial of Service | MedAE O(N) heap state growth | low | accept | Documented; bounded by row count; StateDestroy frees the vector |
| T-01-03-04 | Tampering | npm/pip/cargo installs | n/a | accept | No package installs; RESEARCH.md Package Legitimacy Audit records zero packages |

No high-severity threats (ASVS L1). Regression aggregates operate on plain numeric columns with no external input surface.
</threat_model>

<verification>
- All regression golden queries pass against committed sklearn values.
- R² returns 1.0/0.0 (never NaN/Inf) on constant-target fixtures.
- MAPE skips zero-actual rows and returns NULL when all actuals are zero.
- MedAE returns the correct median for odd and even N.
- Each user-facing function has exactly one telemetry call at bind.
</verification>

<success_criteria>
RMET-01..04 implemented, golden-verified against scikit-learn, with documented non-crashing edge-case behavior (constant target, zero actuals).
</success_criteria>

<output>
Create `.planning/phases/01-evaluation-metrics-cross-validation/01-03-SUMMARY.md` when done.
</output>
