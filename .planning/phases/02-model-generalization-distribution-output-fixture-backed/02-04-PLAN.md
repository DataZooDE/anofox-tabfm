---
phase: 02-model-generalization-distribution-output-fixture-backed
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - src/tabfm_weights.cpp
  - test/sql/tabfm_license.test
  - test/sql/tabfm_distribution.test
autonomous: true
requirements: [MODL-03]

estimate:
  tokens: 60000
  raw_tokens: 32000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Each non-tabfm-v1 family enforces its own license-acceptance gate before download, keyed on the manifest license id, with an error naming the exact SET to run (MODL-03)"
    - "The existing anofox_tabfm_accept_hf_license gate still works for tabfm-non-commercial-v1.0 (backward compat)"
    - "The ungated fixture license (fixture-mit) fires no gate — the tabpfn_v2 fixture predicts without acceptance"
    - "tabfm_regress(..., opts:={'output_mode':'distribution'}) on the committed tabpfn_v2 fixture returns yhat_dist STRUCT(logits,borders) and yhat_quantiles matching golden.json, end-to-end through registry + distribution decode"
  artifacts:
    - test/sql/tabfm_distribution.test
  key_links:
    - "tabfm_weights.cpp RequireLicenseAccepted → GenericLicenseAccepted(manifest.license) → per-license option pre-registered in plan 01"
    - "tabfm_distribution.test SET model_manifest=tabpfn_v2 fixture → output_mode='distribution' → yhat_dist + yhat_quantiles vs golden"
---

<objective>
Generalize the license gate so every family enforces its own acceptance keyed on the manifest license id (MODL-03), and prove the whole phase end-to-end: predict on the committed tabpfn_v2 fixture with output_mode='distribution' through the plan-01 registry and plan-02 decode, asserting yhat_dist + yhat_quantiles against plan-03's golden.

Purpose: Close MODL-03 and land the capstone end-to-end SQL verification that ties registry + distribution + fixture together.
Output: Generic license gate + error-path test, and the end-to-end distribution SQL test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-model-generalization-distribution-output-fixture-backed/02-RESEARCH.md
@.planning/phases/02-model-generalization-distribution-output-fixture-backed/02-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generic manifest-license-keyed gate + error-path test (MODL-03)</name>
  <files>src/tabfm_weights.cpp, test/sql/tabfm_license.test</files>
  <read_first>
    - src/tabfm_weights.cpp lines 95-101 (LicenseAccepted) and 285-295 (RequireLicenseAccepted + InvalidConfigurationException shape)
    - src/tabfm_weights.cpp around lines 63, 291 (IsGated + gate call sites)
    - .planning/phases/02-model-generalization-distribution-output-fixture-backed/02-PATTERNS.md "src/tabfm_weights.cpp" (SanitizeLicenseId + GenericLicenseAccepted + RequireLicenseAccepted replacement)
    - .planning/phases/02-model-generalization-distribution-output-fixture-backed/02-RESEARCH.md §6 (backward-compat alias, error text, sanitize rule)
    - test/sql/tabfm_license.test lines 1-90 (existing gate error-path structure + weights_test fixtures)
    - test/fixtures/weights_test/ manifests (manifest_ok.json gated, manifest_free.json ungated)
  </read_first>
  <action>
Per MODL-03 (PATTERNS + RESEARCH §6). In `src/tabfm_weights.cpp` (anonymous namespace):
- Add `string SanitizeLicenseId(const string &id)`: lowercase, replace every non-alphanumeric char with `_`.
- Add `bool GenericLicenseAccepted(ClientContext &context, const string &license_id)`: return true for empty/`none`; for `tabfm-non-commercial-v1.0` also accept via the legacy `LicenseAccepted(context)` (backward compat); otherwise look up `anofox_tabfm_accept_<sanitized>` via TryGetCurrentSetting (options pre-registered in plan 01) and return its boolean.
- Replace `RequireLicenseAccepted` to call `GenericLicenseAccepted(context, manifest.license)` and, on failure, throw `InvalidConfigurationException` naming the family and `Run: SET anofox_tabfm_accept_<sanitized_id> = true;` (SQL-API §5). Keep the legacy `anofox_tabfm_accept_hf_license` error text path working for the tabfm-non-commercial-v1.0 family so the EXISTING license.test assertions still match (do not break the substring the current test greps).
- Keep the existing `IsGated()` semantics: `fixture-mit` / `none` are ungated (no gate fires on the tabpfn_v2 fixture).

`test/sql/tabfm_license.test`: KEEP all existing assertions (backward compat). APPEND a section for a second license family: point the manifest at a gated non-hf license id (add a small `test/fixtures/weights_test/manifest_gated_other.json` if one does not exist, or reuse an existing gated manifest with a distinct license), assert `statement error` names the correct `SET anofox_tabfm_accept_<sanitized> = true;` substring while unaccepted, and that accepting the matching option clears the gate. Match SUBSTRINGS only (comment-text discipline), never the full sentence.
  </action>
  <verify>
    <automated>make debug && ./build/debug/test/unittest test/sql/tabfm_license.test</automated>
    <fails_when>The gate regresses — the existing tabfm-non-commercial-v1.0 gate stops firing/clearing, or the new per-family gate does not name the correct SET option, or an ungated fixture license wrongly triggers a gate.</fails_when>
  </verify>
  <done>Generic license gate keyed on manifest license id fires with a named SET before download; legacy hf-license path unchanged; ungated fixture licenses fire no gate (MODL-03).</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end distribution predict on the tabpfn_v2 fixture vs golden (capstone)</name>
  <files>test/sql/tabfm_distribution.test</files>
  <read_first>
    - test/sql/tabfm_regress.test (SET anofox_tabfm_model_manifest, tabfm_regress single/two-table forms, DESCRIBE column checks, unnest usage)
    - test/fixtures/tabpfn_v2/manifest.json + golden.json (committed by plan 03 — the fixture to load and the expected decoded values)
    - .planning/phases/02-model-generalization-distribution-output-fixture-backed/02-PATTERNS.md "No Analog Found" (tabfm_distribution.test format guidance)
    - CLAUDE.md rule: unnest(res, max_depth := 3) — never recursive := true
  </read_first>
  <action>
Write the end-to-end SQL test proving the phase's headline capability. Header per tabfm_regress.test (require anofox_tabfm; LOAD; SET anofox_tabfm_model_manifest = 'test/fixtures/tabpfn_v2/manifest.json'). Build a small regression table matching the fixture's expected feature width (H) with some NULL-target test rows.

Assertions:
- `tabfm_regress(t, target, opts := {'output_mode':'distribution'})` returns rows; DESCRIBE shows `yhat_dist` and `yhat_quantiles` columns present (they exist ONLY under distribution — plan 02 gating).
- The DEFAULT (compact) `tabfm_regress(t, target)` DESCRIBE does NOT contain `yhat_dist`/`yhat_quantiles` (backward compat).
- `yhat_dist.borders` has length K+1 (17) and `yhat_dist.logits` has length K (16); `yhat_quantiles` has 9 elements; use `len(...)`/struct field access (unnest with max_depth := 3 if needed — never recursive := true).
- Numeric check: for the scored rows, the decoded mean (yhat) and the quantiles are finite and monotonically non-decreasing across the 9 levels; where the fixture inputs are deterministic, cross-check the mean/quantiles against golden.json values within a tolerance (e.g. abs diff < 1e-3), matching the golden the plan-03 numpy reference produced. (Predictions are from random-init weights — meaningless as forecasts — but the decode pipeline is exact and golden-checked.)
- Determinism: two identical distribution calls agree on yhat.

This test transits registry dispatch (plan 01) → tabpfn_v2 preprocess (plan 03) → ORT borders read + ValidateDistributionOutput (plan 02) → distribution decode (plan 02) → bind return type (plan 02) end-to-end.
  </action>
  <verify>
    <automated>./build/debug/test/unittest test/sql/tabfm_distribution.test</automated>
    <fails_when>End-to-end distribution predict fails — yhat_dist/yhat_quantiles columns absent under distribution, present under default, wrong list lengths, non-monotone quantiles, or mean/quantiles diverging from golden.json beyond tolerance.</fails_when>
  </verify>
  <verify>
    <automated>make test_debug</automated>
    <fails_when>Any test in the full suite fails — a phase-2 change regressed another module.</fails_when>
  </verify>
  <done>output_mode='distribution' on the committed tabpfn_v2 fixture returns yhat_dist + yhat_quantiles matching golden within tolerance; compact default omits them; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SET license option → download gate | Untrusted license id from manifest is used to construct an option name and gate a network download |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Elevation of Privilege | license-gate bypass for a new family | high | mitigate | RequireLicenseAccepted uses GenericLicenseAccepted keyed on the manifest license id; a gated family cannot download without its specific pre-registered option accepted; gate fires at bind before any file/network I/O (mirrors existing license.test "no cache dir appears" guarantee). |
| T-02-08 | Elevation of Privilege | option-name injection via crafted license id | medium | mitigate | SanitizeLicenseId lowercases and replaces every non-alphanumeric char with underscore before constructing the setting name, so a crafted id cannot form an unexpected/valid option name. |

No high-severity residuals: the gate fails closed for any gated family lacking its accepted option.
</threat_model>

<verification>
- `./build/debug/test/unittest test/sql/tabfm_license.test` proves MODL-03 (per-family gate + legacy compat + ungated fixture).
- `./build/debug/test/unittest test/sql/tabfm_distribution.test` proves the end-to-end distribution path against golden.
- `make test_debug` proves no cross-module regression.
</verification>

<success_criteria>
- Per-family license gate keyed on manifest license id, error names the exact SET (MODL-03).
- Legacy hf-license gate unchanged; ungated fixture fires no gate.
- End-to-end distribution predict on the tabpfn_v2 fixture matches golden; compact default unchanged.
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/02-model-generalization-distribution-output-fixture-backed/02-04-SUMMARY.md` when done
</output>
