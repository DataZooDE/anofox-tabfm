---
phase: 02-model-generalization-distribution-output-fixture-backed
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tools/parity/pyproject.toml
  - tools/parity/src/parity/__init__.py
  - tools/parity/src/parity/check_tabpfn_v2.py
  - tools/parity/src/parity/build_fixture.py
  - tools/parity/tests/test_check_tabpfn_v2.py
  - test/fixtures/tabpfn_v2/manifest.json
  - test/fixtures/tabpfn_v2/graph_tabpfn_v2.onnx
  - test/fixtures/tabpfn_v2/model.safetensors
  - test/fixtures/tabpfn_v2/tensor_map_tabpfn_v2.json
  - test/fixtures/tabpfn_v2/golden.json
  - test/fixtures/tabpfn_v2/FIXTURE_SHA256
  - src/tabfm_preprocess_tabpfn_v2.cpp
autonomous: true
requirements: [MODL-01, MODL-04]

estimate:
  tokens: 80000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A committed weight-free random-init tabpfn_v2 fixture family exists (manifest + tabpfn_v2 preprocessing profile + K=16 ONNX graph emitting logits [n,K] and borders [K+1] non-uniform), with no model weight bytes in the repo (license wall)"
    - "tools/parity validates the fixture's TWO distribution tensors (logits [T,K] + borders [K+1], borders strictly increasing) against the confirmed contract before the C++ decoder is trusted (MODL-04)"
    - "The tabpfn_v2 preprocessing profile produces a fixture-scoped PreprocessedBatch (correct T/H/train_size/x/y/target_mean/target_scale) that the engine can feed to the fixture graph (MODL-01)"
    - "A golden fixture records the fixture's logits+borders and the Python-derived decoded mean/quantiles for the C++ end-to-end test (used by plan 04)"
    - "CI verifies the fixture sha256 and never regenerates it (deterministic, double-build byte-equal)"
  artifacts:
    - tools/parity/pyproject.toml
    - tools/parity/src/parity/check_tabpfn_v2.py
    - tools/parity/src/parity/build_fixture.py
    - test/fixtures/tabpfn_v2/manifest.json
    - test/fixtures/tabpfn_v2/graph_tabpfn_v2.onnx
    - test/fixtures/tabpfn_v2/golden.json
    - test/fixtures/tabpfn_v2/FIXTURE_SHA256
  key_links:
    - "manifest.json distribution_output=true + preprocessing_profile='tabpfn_v2' → engine distribution path (plan 02) on this fixture"
    - "ONNX graph outputs are explicitly named 'logits' and 'borders' (Pitfall 3) so plan 02's ORT Run reads both"
    - "tabpfn_v2 profile self-registers via the ProfileRegistration in this .cpp (stub created in plan 01, filled here)"
---

<objective>
Deliver the weight-free tabpfn_v2 fixture family (MODL-01) and the parity tool that validates its ONNX distribution contract before the C++ decoder is trusted (MODL-04), plus fill the fixture-scoped tabpfn_v2 preprocessing profile stub from plan 01. K=16 keeps the fixture tiny and license-wall-clean; the decoder (plan 02) handles arbitrary K.

Purpose: Give the phase a concrete, committed distribution-emitting model to exercise the plan-01 registry and plan-02 decode path end-to-end, with a Python parity gate proving the two tensors match the confirmed contract.
Output: tools/parity uv project, committed K=16 fixture (graph, safetensors, manifest, golden, sha256), and the filled tabpfn_v2 profile.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-model-generalization-distribution-output-fixture-backed/02-RESEARCH.md
@.planning/phases/02-model-generalization-distribution-output-fixture-backed/02-PATTERNS.md
@.planning/spikes/SPIKE-tabpfn-v2-tensor-contract.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: tools/parity uv project — fixture builder + contract validator (MODL-04)</name>
  <files>tools/parity/pyproject.toml, tools/parity/src/parity/__init__.py, tools/parity/src/parity/build_fixture.py, tools/parity/src/parity/check_tabpfn_v2.py, tools/parity/tests/test_check_tabpfn_v2.py</files>
  <read_first>
    - tools/export_onnx/pyproject.toml (uv project + [project.scripts] + hatchling layout)
    - tools/make_fixture/src/make_fixture/fixture.py (seeded random-init, single-metadata-key safetensors, weight-free strip + assert, sha256_file, FIXTURE_SHA256 format, double-build determinism)
    - .planning/phases/02-model-generalization-distribution-output-fixture-backed/02-RESEARCH.md §5 (hand-built onnx.helper graph recipe; parity check code) and Pitfall 3/7 (named outputs; Constant node output wired to graph.output)
    - .planning/phases/02-model-generalization-distribution-output-fixture-backed/02-PATTERNS.md "tools/parity/" + "test/fixtures/tabpfn_v2/" sections
  </read_first>
  <precondition>Python 3.11+ and uv are available (uv run works inside tools/make_fixture) — external tool setup.</precondition>
  <action>
Create the `tools/parity` uv project (MODL-04) mirroring tools/export_onnx layout (PATTERNS): pyproject.toml with dependencies onnx>=1.17, onnxruntime>=1.20, numpy, dev group pytest>=8, [project.scripts] entries for build + check, hatchling build, packages=["src/parity"]. NO torch dependency.

`build_fixture.py`: hand-build the K=16 weight-free ONNX graph with `onnx.helper` per RESEARCH §5. Graph accepts the engine's existing input convention (x [1,T,H], y [1,T], train_size [1], cat_mask [1,H], d [1]) so plan 02's ORT Run and the engine feed it unchanged (RESEARCH open-question 3: keep [1,T,H]). Internally reshape x to [T,H] and produce `logits` [T,K] via a small linear layer (weights from numpy default_rng(seed=42), stored as ONNX initializers referenced by tensor_map, matching the existing injection pattern). Emit a SECOND graph output named exactly `borders` [K+1] — build a NON-UNIFORM strictly-increasing border vector (sorted, widths deliberately varied to mimic the real contract's non-uniformity) as a Constant node whose output is added to graph.output (Pitfall 7). Explicitly name both outputs `logits` and `borders` (Pitfall 3). Then strip weight data (zero initializers) and assert weight-free (mirror make_fixture's delete_weight_data/assert_weight_free — implement a local strip if x_export is unavailable). Write model.safetensors (single __metadata__ key, sorted keys — determinism), tensor_map_tabpfn_v2.json, and the fixture files. Compute sha256 for each artifact and write FIXTURE_SHA256 in the existing one-line-per-file format. Double-build and assert byte-equality before finalizing.

`check_tabpfn_v2.py`: implement `check_tabpfn_v2_contract(fixture_dir, K=16)` per RESEARCH §5 — load manifest, InferenceSession, assert both `logits` and `borders` are in output names, run with minimal inputs, assert logits.shape == (T,K), borders.shape == (K+1,), and borders strictly increasing (np.all(np.diff > 0)). `main()` runs it against test/fixtures/tabpfn_v2 and exits nonzero on failure.

`tests/test_check_tabpfn_v2.py`: pytest that runs check_tabpfn_v2_contract against the committed fixture and asserts it passes; add a negative test that a mutated (non-increasing borders) array fails the check.
  </action>
  <verify>
    <automated>cd tools/parity && uv run pytest -q</automated>
    <fails_when>The parity check fails — the fixture graph is missing the 'logits'/'borders' named outputs, shapes deviate from [T,K]/[K+1], or borders are not strictly increasing.</fails_when>
  </verify>
  <verify>
    <automated>cd tools/parity && uv run check_tabpfn_v2</automated>
    <fails_when>The committed fixture does not satisfy the [n,K]+[K+1] contract (nonzero exit).</fails_when>
  </verify>
  <done>tools/parity exists; pytest green; check_tabpfn_v2 confirms the committed fixture emits both named distribution tensors with the contracted shapes and monotone borders.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the K=16 weight-free tabpfn_v2 fixture family + golden.json (MODL-01)</name>
  <files>test/fixtures/tabpfn_v2/manifest.json, test/fixtures/tabpfn_v2/graph_tabpfn_v2.onnx, test/fixtures/tabpfn_v2/model.safetensors, test/fixtures/tabpfn_v2/tensor_map_tabpfn_v2.json, test/fixtures/tabpfn_v2/golden.json, test/fixtures/tabpfn_v2/FIXTURE_SHA256</files>
  <read_first>
    - test/fixtures/regression/manifest.json (exact manifest schema to follow: manifest_version, model_id, task, license, repo, revision, graph, graph_id, tensor_map, preprocessing_profile, engine_profiles, files[])
    - test/fixtures/FIXTURE_SHA256 (one-line-per-file format)
    - .planning/phases/02-model-generalization-distribution-output-fixture-backed/02-PATTERNS.md "test/fixtures/tabpfn_v2/" (manifest additions: preprocessing_profile 'tabpfn_v2', distribution_output true, license 'fixture-mit')
    - .planning/spikes/SPIKE-tabpfn-v2-tensor-contract.md "Decode Math" (to derive golden mean/quantiles)
  </read_first>
  <action>
Run the plan-01 Task-1 builder to generate the fixture, then commit the artifacts (MODL-01). manifest.json follows the regression fixture schema EXACTLY plus: `"preprocessing_profile": "tabpfn_v2"`, `"distribution_output": true`, `"license": "fixture-mit"` (ungated — IsGated() false, so no license gate fires on the fixture; the real tabpfn_v2 gated license is handled by plan 04). Set repo `local:test/fixtures/tabpfn_v2`, task `regression`. Populate files[] with the real bytes + sha256 for model.safetensors, graph_tabpfn_v2.onnx, tensor_map_tabpfn_v2.json.

golden.json: record the fixture's raw logits (n_test rows × K=16), the z-space borders (K+1=17, non-uniform), a chosen (y_mean, y_std) affine pair, and the Python-derived decoded mean + the 9 quantiles at {0.1..0.9} in RAW space (compute with a numpy reference implementing the spike decode math incl. outer-bin half-normal correction). Document the derivation in a top-level comment/field (traceable like Phase 1 goldens; not a build artifact). This golden is consumed by plan 04's end-to-end SQL test AND cross-checks plan 02's C++ decoder.

FIXTURE_SHA256: one `filename sha256` line per committed artifact, matching the existing format; CI verifies these and never regenerates (STATE decision: committed fixtures with deterministic sha256).

Verify the fixture is weight-free: no nonzero model-weight initializers, no Google/TabPFN weight bytes (license wall — random-init only).
  </action>
  <verify>
    <automated>cd tools/parity && uv run check_tabpfn_v2 && cd /home/simonm/projects/duckdb/anofox-tabfm && sha256sum -c test/fixtures/tabpfn_v2/FIXTURE_SHA256</automated>
    <fails_when>The committed fixture fails the contract check, or any artifact's sha256 does not match FIXTURE_SHA256 (nondeterministic build or tampered file).</fails_when>
  </verify>
  <verify>
    <automated>cd tools/parity && uv run python -c 'import onnx; m=onnx.load("../../test/fixtures/tabpfn_v2/graph_tabpfn_v2.onnx"); names=set(o.name for o in m.graph.output); assert set(["logits","borders"]).issubset(names), names; print("outputs", names)'</automated>
    <fails_when>The committed ONNX graph does not declare both named outputs 'logits' and 'borders' (Pitfall 3/7 regression).</fails_when>
  </verify>
  <done>The tabpfn_v2 fixture family is committed weight-free with distribution_output=true and preprocessing_profile='tabpfn_v2'; parity + sha256 verification pass; the graph declares both named outputs; golden.json carries logits+borders+decoded mean/quantiles.</done>
</task>

<task type="auto">
  <name>Task 3: Fill the fixture-scoped tabpfn_v2 preprocessing profile (MODL-01)</name>
  <files>src/tabfm_preprocess_tabpfn_v2.cpp</files>
  <read_first>
    - src/tabfm_preprocess_tabpfn_v2.cpp (the stub created in plan 01 — self-registration + ForceTabPFNV2ProfileInit already present)
    - src/include/tabfm_preprocess.hpp (PreprocessedBatch fields: T, H, d, train_size, x, y, cat_mask, target_mean, target_scale, label_decoder, row_source_index, kTargetPadSentinel)
    - src/tabfm_preprocess.cpp (how PreprocessBatch populates the batch for regression — the reference for filling the fixture-scoped fields)
    - .planning/phases/02-model-generalization-distribution-output-fixture-backed/02-RESEARCH.md §1 "tabpfn_v2 Preprocessing Profile (fixture-scoped)" (required fields) and the code example at lines 880-896
  </read_first>
  <action>
Fill `TabPFNV2PreprocessBatch` (currently the plan-01 stub that throws NotImplementedException). Per RESEARCH §1: a fixture-scoped MINIMAL profile that passes features through as-is (no z-score / no categorical encoding — the full-fidelity tabpfn_v2 preprocessing is deferred with the real export). Populate the PreprocessedBatch so the engine can feed the fixture graph: T=total rows, H=feature width, d=feature count (=H for fixture), train_size=count of training rows, x=row-major features (train rows first, test rows last — matching the batch layout the engine expects and row_source_index bookkeeping), y=training targets with kTargetPadSentinel for test rows, target_mean/target_scale = mean/std of training targets (used by plan 02's affine transform to raw space), label_decoder empty (regression), cat_mask all-false (fixture has no categoricals). Keep the self-registration line (`static const ProfileRegistration kTabPFNV2Reg("tabpfn_v2", TabPFNV2PreprocessBatch);`) and `void ForceTabPFNV2ProfileInit() {}` from the plan-01 stub. Add a file-level comment: `// MODL-01 fixture-scoped; full-fidelity tabpfn_v2 preprocessing deferred (real-export work)` and reference RESEARCH §1. Do NOT edit CMakeLists (plan 01 already wired this .cpp).
  </action>
  <verify>
    <automated>make debug</automated>
    <fails_when>Build fails — a PreprocessedBatch field the engine reads is left unset or mis-typed, or the self-registration line was dropped (link error on kTabPFNV2Reg / ForceTabPFNV2ProfileInit).</fails_when>
  </verify>
  <done>The tabpfn_v2 profile self-registers and returns a valid fixture-scoped PreprocessedBatch; make debug succeeds. End-to-end predict on the fixture is exercised in plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture files → build/CI | Fixture artifacts must be weight-free and byte-deterministic; a tampered/regenerated fixture crosses into tests |
| fixture ONNX graph → ORT | The graph's declared outputs cross into plan 02's ORT Run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | committed fixture bytes | medium | mitigate | FIXTURE_SHA256 pins every artifact; CI verifies and never regenerates; double-build byte-equality asserted before commit. |
| T-02-06 | Tampering | ONNX output contract drift | high | mitigate | tools/parity (MODL-04) asserts logits [T,K] + borders [K+1] strictly-increasing named outputs BEFORE the C++ decoder is trusted. |
| T-02-SC | Tampering | pip installs in tools/parity | low | accept | Only already-approved packages (onnx, onnxruntime, numpy) per RESEARCH Package Legitimacy Audit — no new/unaudited packages; no [ASSUMED]/[SUS] entries. |

No high-severity residuals: the ONNX contract is parity-gated and the fixture is sha256-pinned. License wall upheld — fixture is random-init, weight-free.
</threat_model>

<verification>
- `cd tools/parity && uv run pytest -q` proves the parity tool + fixture contract (MODL-04).
- `sha256sum -c test/fixtures/tabpfn_v2/FIXTURE_SHA256` proves the fixture is committed and byte-stable.
- `make debug` proves the tabpfn_v2 profile compiles and self-registers (MODL-01).
</verification>

<success_criteria>
- Weight-free K=16 tabpfn_v2 fixture family committed (MODL-01), no model weight bytes (license wall).
- tools/parity validates both distribution tensors before the decoder is trusted (MODL-04).
- tabpfn_v2 preprocessing profile self-registers and yields a valid fixture-scoped batch.
- golden.json ready for plan 04's end-to-end test.
</success_criteria>

<output>
Create `.planning/phases/02-model-generalization-distribution-output-fixture-backed/02-03-SUMMARY.md` when done
</output>