---
phase: 03-proper-scoring-rules-cross-model-comparison
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/tabfm_scoring.cpp
  - test/cpp/test_tabfm_scoring.cpp
  - test/sql/tabfm_scoring.test
  - tools/parity/src/parity/crps_reference.py
  - src/tabfm_crossval.cpp
autonomous: true
requirements: [PSR-02, PSR-03, CMP-01]
estimate:
  tokens: 82000
  raw_tokens: 41000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "SELECT tabfm_log_score(actual, yhat_dist) returns mean NLL = mean(log(w_i) - log(p_i)) over the containing bin, matching the golden reference (PSR-02)"
    - "tabfm_log_score returns the max-penalty -log(epsilon) for actuals outside [b_0, b_K] and for degenerate/zero-mass bins (no log(0), no divide-by-zero)"
    - "tabfm_interval_score(actual, yhat_dist, coverage := 0.9) computes (u-l) + (2/alpha)*(underflow+overflow) with l,u the coverage quantiles, matching the golden reference (PSR-03)"
    - "tabfm_interval_score rejects coverage <= 0 or >= 1 at bind with a named error"
    - "tabfm_log_score and tabfm_interval_score are bind-gated on the distribution STRUCT; plain DOUBLE second argument throws with the output_mode := 'distribution' remedy (PSR-04 extended)"
    - "A single SQL macro compares tabpfn_v2 (fixture) distribution scores against tabfm-v1 point metrics on the user's own table, quoting the target identifier safely (CMP-01)"
    - "Full existing suite (make test_debug) passes"
  artifacts:
    - "src/tabfm_scoring.cpp — tabfm_log_score and tabfm_interval_score aggregate sets added"
    - "src/tabfm_crossval.cpp — tabfm_compare_models macro added to RegisterCrossValidateMacros"
    - "test/cpp/test_tabfm_scoring.cpp — log-score + interval-score unit cases"
    - "test/sql/tabfm_scoring.test — PSR-02/PSR-03 golden + bind-gate + coverage-range + CMP-01 blocks"
  key_links:
    - "tabfm_interval_score reuses DistributionQuantile (tabfm_predict.hpp:206, external linkage) for the coverage quantiles"
    - "coverage constant is captured into IScoreBindData at bind (validated 0<coverage<1) and read in Update via aggr_input.bind_data"
    - "tabfm_compare_models is registered inside RegisterCrossValidateMacros (no new scaffold file)"
---

<objective>
Expand the scoring module out from the Plan-A CRPS tracer: add `tabfm_log_score` (PSR-02),
`tabfm_interval_score` (PSR-03) — both bind-gated like CRPS (PSR-04) and golden-tested — plus the
`tabfm_compare_models` cross-model comparison macro (CMP-01) that runs Phase-1 metrics and Phase-3
scores across the tabpfn_v2 fixture distribution and tabfm-v1 on the user's own table.

Purpose: With the STRUCT-read + bind-gate + golden pipeline proven in Plan A, each new score reuses
that spine. Interval score additionally reuses the existing DistributionQuantile helper and
introduces the coverage bind-data pattern. CMP-01 composes the model-agnostic primitives via a thin
safe-quoting SQL macro — no new C++ compute.

Output: two more scoring aggregates, the comparison macro, their unit + SQL golden tests, and a
green full suite.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-proper-scoring-rules-cross-model-comparison/03-RESEARCH.md
@.planning/phases/03-proper-scoring-rules-cross-model-comparison/03-PATTERNS.md
@.planning/phases/03-proper-scoring-rules-cross-model-comparison/03-CONTEXT.md

# Plan A output (this plan extends the same module + tests)
@.planning/phases/03-proper-scoring-rules-cross-model-comparison/03-01-SUMMARY.md
@src/tabfm_scoring.cpp

# Analog source (read cited ranges only)
@src/tabfm_metrics_classification.cpp
@src/include/tabfm_predict.hpp
@src/tabfm_crossval.cpp
@test/sql/tabfm_distribution.test
@test/fixtures/tabpfn_v2/golden.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tabfm_log_score aggregate (PSR-02) + bind-gate + golden/unit tests</name>
  <files>src/tabfm_scoring.cpp, test/cpp/test_tabfm_scoring.cpp, test/sql/tabfm_scoring.test, tools/parity/src/parity/crps_reference.py</files>
  <read_first>
    - src/tabfm_scoring.cpp (Plan A) — the CRPS aggregate triplet, STRUCT-read idiom, SoftmaxInPlace, and the fn_dist+fn_point bind-gate registration to replicate.
    - 03-RESEARCH.md "Log-Score Math (PSR-02)" — density = p_i/w_i; log_score = log(w_i) - log(max(clip_eps, p_i)); clip_eps = 1e-10; out-of-support y and w_i<1e-300 → -log(clip_eps). Document the epsilon (assumption A2).
    - 03-RESEARCH.md "Common Pitfalls" 4 (zero-width bins) and 7 (y outside [b_0,b_K]).
  </read_first>
  <action>
    In src/tabfm_scoring.cpp add (anonymous namespace or external-linkage per Plan A's choice):
    `double ComputeLogScore(double y, const std::vector<double> &probs, const std::vector<double> &borders)`
    with clip_eps = 1e-10 (named constant + comment citing A2): if y < borders.front() or y > borders.back()
    return -std::log(clip_eps); else find bin i with b_i <= y <= b_{i+1}; wi = borders[i+1]-borders[i];
    if wi < 1e-300 return -std::log(clip_eps); return std::log(wi) - std::log(std::max(clip_eps, probs[i])).
    Add LogScoreState {double sum_ls; int64_t n} + StateSize/Init/Update/Combine/Finalize mirroring CRPS
    (Update: same STRUCT-read + SoftmaxInPlace, sum_ls += ComputeLogScore). LogScoreBind: telemetry
    "tabfm_log_score". Register anofox_tabfm_log_score set with fn_dist FIRST then fn_point (bind throws
    with the output_mode := 'distribution' remedy — PSR-04 extended). FunctionDescription + example.
    RegisterAggregateFunctionSetWithAlias(..., "tabfm_log_score", ...).

    Extend tools/parity/src/parity/crps_reference.py with a documented `log_score_bar(y, logits, borders)`
    (same softmax; density = p_i/w_i; clip 1e-10; out-of-support → -log(1e-10)) and print its value for
    the synthetic K=4 case and the fixture, for the tests to hard-code.

    Add Catch2 cases in test/cpp/test_tabfm_scoring.cpp: ComputeLogScore for the K=4 synthetic (y in a
    bin) and an out-of-support y → -log(1e-10), matching the Python reference within 1e-9.
    Add PSR-02 blocks in test/sql/tabfm_scoring.test: golden mean log-score over preds within round(,6);
    alias parity; PSR-04 `statement error` for plain DOUBLE with the named remedy. Comment with PSR-02.
  </action>
  <behavior>
    - ComputeLogScore(K=4 synthetic, y inside bin) == log_score_bar reference within 1e-9.
    - Out-of-support y and zero-width bin both return -log(1e-10) (finite, no NaN/Inf).
  </behavior>
  <verify>
    <automated>cd tools/parity && uv run python -m parity.crps_reference && cd /home/simonm/projects/duckdb/anofox-tabfm && make debug && ./build/debug/test/unittest "[tabfm_scoring]" && ./build/debug/test/unittest test/sql/tabfm_scoring.test</automated>
    <fails_when>Catch2 or sqllogictest reports a log-score mismatch, or the PSR-04 error block for tabfm_log_score does not see the named remedy; SUCCESS = all [tabfm_scoring] + tabfm_scoring.test blocks pass.</fails_when>
  </verify>
  <done>tabfm_log_score / anofox_tabfm_log_score registered and golden-correct (PSR-02); out-of-support and zero-width bins return the documented max penalty; point-estimate call rejected at bind.</done>
  <reversibility rating="reversible">Additive aggregate + tests.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tabfm_interval_score aggregate with coverage bind-data (PSR-03) + tests</name>
  <files>src/tabfm_scoring.cpp, test/cpp/test_tabfm_scoring.cpp, test/sql/tabfm_scoring.test, tools/parity/src/parity/crps_reference.py</files>
  <read_first>
    - 03-RESEARCH.md "Interval Score Math (PSR-03)" — alpha=1-coverage; q_lo=alpha/2; q_hi=1-alpha/2; l/u via DistributionQuantile; score = (u-l) + (2/alpha)*((l-y)*[y<l] + (y-u)*[y>u]); default coverage 0.9; valid (0,1) exclusive, throw BinderException at bind otherwise.
    - src/tabfm_metrics_classification.cpp lines 205-248 — F1BindData FunctionData subclass (Copy/Equals) + ExpressionExecutor::EvaluateScalar reading a constant arg at bind. Lines 335-364 — reading bind_data->Cast<...>() in Finalize/Update.
    - src/include/tabfm_predict.hpp line 206 — DistributionQuantile(probs, borders, q) signature (external linkage; already included in tabfm_scoring.cpp).
    - 03-RESEARCH.md "Common Pitfalls" 5 (coverage must be captured in bind data — Update has no ClientContext).
  </read_first>
  <action>
    In src/tabfm_scoring.cpp add `struct IScoreBindData : FunctionData { double coverage; ... Copy()/Equals() }`
    (F1BindData shape). IScoreBind: telemetry "tabfm_interval_score"; if a 3rd (coverage) argument is
    present, read it via ExpressionExecutor::EvaluateScalar(context, *arguments[2]) → double; else default
    0.9; validate 0.0 < coverage < 1.0 else throw InvalidInputException naming the coverage param and its
    valid range; return make_uniq<IScoreBindData>(coverage). Add
    `double ComputeIntervalScore(double y, const std::vector<double> &probs, const std::vector<double> &borders, double coverage)`:
    alpha=1-coverage; l=DistributionQuantile(probs,borders,alpha/2); u=DistributionQuantile(probs,borders,1-alpha/2);
    return (u-l) + (2.0/alpha)*((y<l ? (l-y):0.0) + (y>u ? (y-u):0.0)). IScoreState {double sum_is; int64_t n}.
    IScoreUpdate reads coverage via aggr_input.bind_data->Cast<IScoreBindData>().coverage, then the same
    STRUCT-read + SoftmaxInPlace + sum_is += ComputeIntervalScore. Combine/Finalize mirror CRPS.
    Register anofox_tabfm_interval_score set: fn_dist2 ({DOUBLE, yhat_dist}→DOUBLE, IScoreBind, default
    coverage) and fn_dist3 ({DOUBLE, yhat_dist, DOUBLE}→DOUBLE, IScoreBind reads arg[2]) — both FIRST;
    then fn_point ({DOUBLE, DOUBLE}→DOUBLE) whose bind throws the output_mode remedy (PSR-04). Named
    parameter `coverage` works via the 3-arg overload (users call `coverage := 0.9`). FunctionDescription
    + example `SELECT tabfm_interval_score(actual, yhat_dist, coverage := 0.9) FROM ...`.
    RegisterAggregateFunctionSetWithAlias(..., "tabfm_interval_score", ...).

    Extend crps_reference.py with `interval_score_bar(y, logits, borders, coverage)` reusing a Python
    quantile matching DistributionQuantile (CDF cumsum + linear interpolation over non-uniform bins,
    per the SPIKE icdf math) and print values for coverage 0.9 and 0.5 on the synthetic + fixture cases.

    Catch2: ComputeIntervalScore for the K=4 synthetic at coverage 0.9 and 0.5 matching the Python
    reference within 1e-9. SQL: PSR-03 golden blocks (default 0.9 and explicit coverage := 0.5) within
    round(,6); alias parity; `statement error` for coverage := 0.0 and coverage := 1.0 (named range
    error); PSR-04 `statement error` for plain DOUBLE. Comment with PSR-03.
  </action>
  <behavior>
    - ComputeIntervalScore(K=4, coverage 0.9) == interval_score_bar reference within 1e-9; likewise 0.5.
    - coverage := 0.0 and coverage := 1.0 throw at bind naming the valid range.
    - Under-coverage (y outside [l,u]) increases the score via the 2/alpha penalty.
  </behavior>
  <verify>
    <automated>make debug && ./build/debug/test/unittest "[tabfm_scoring]" && ./build/debug/test/unittest test/sql/tabfm_scoring.test</automated>
    <fails_when>interval-score value mismatch (Catch2/sqllogictest FAILED), or the coverage-range / PSR-04 error blocks miss their named text; SUCCESS = all [tabfm_scoring] + tabfm_scoring.test blocks pass.</fails_when>
  </verify>
  <done>tabfm_interval_score registered with default and explicit coverage (PSR-03), golden-correct, coverage validated at bind, point estimates rejected.</done>
  <reversibility rating="reversible">Additive aggregate + tests.</reversibility>
</task>

<task type="auto">
  <name>Task 3: tabfm_compare_models cross-model comparison macro (CMP-01) + capstone suite</name>
  <files>src/tabfm_crossval.cpp, test/sql/tabfm_scoring.test</files>
  <read_first>
    - src/tabfm_crossval.cpp lines 59-130 (CVMacroDef struct), 128-250 (CROSS_VALIDATE_MACRO body + query() composition), 209 (`replace(CAST(target AS VARCHAR), '"', '""')` safe quoting), 255-312 (BuildTableMacroFunction / BuildMacroInfo / RegisterCVMacroWithAlias), and the RegisterCrossValidateMacros body (where to add the new macro).
    - 03-RESEARCH.md "CMP-01 Macro Pattern" — compares tabpfn_v2 (fixture, distribution) vs tabfm-v1 (point) on the user's table; returns metric rows with a `model` column; no bundled datasets.
    - test/sql/tabfm_distribution.test lines 8-45 — fixture manifest SET + dist_data table (reuse for the CMP-01 SQL test).
    - test/fixtures/tabpfn_v2/golden.json — for the tabpfn_v2 side of the comparison.
  </read_first>
  <action>
    In src/tabfm_crossval.cpp add a new CVMacroDef `COMPARE_MODELS_MACRO` (positional params
    data, target, actual_col or similar; defaults for coverage) and a table-macro body that UNION-ALLs,
    per model family, a row of `{model, rmse, crps, log_score, interval_score}` computed by composing:
    `tabfm_regress(data, target, opts := MAP{'output_mode':'distribution','model':<family>})` feeding
    `tabfm_rmse(actual, yhat)`, `tabfm_crps(actual, yhat_dist)`, `tabfm_log_score(actual, yhat_dist)`,
    `tabfm_interval_score(actual, yhat_dist, coverage := 0.9)`. For tabfm-v1 (point estimate, no
    distribution) emit rmse and NULL for the distribution-only scores (document this in the macro
    description — tabfm-v1 has no predictive distribution, so PSR scores are NULL for it; the fixture
    tabpfn_v2 gets full scores). Interpolate the target/actual identifiers ONLY via
    `replace(CAST(<ident> AS VARCHAR), '"', '""')` and quote (CMP-01 / CV-04 SQL-injection mitigation).
    Register it inside RegisterCrossValidateMacros via RegisterCVMacroWithAlias as
    anofox_tabfm_compare_models + short alias tabfm_compare_models (no new scaffold file — extends the
    macro-module-owned tabfm_crossval.cpp).

    Add a CMP-01 block to test/sql/tabfm_scoring.test: SET the fixture manifest, use dist_data, call
    `SELECT model, ... FROM tabfm_compare_models('dist_data', 'y_val', ...)` and assert it returns one
    row per model with the tabpfn_v2 CRPS/log-score matching the golden reference (round(,6)) and the
    tabfm-v1 distribution scores NULL. Add an identifier-safety block: a target column name containing
    a double-quote must not break the macro (statement ok or a controlled result), proving the quoting.
    Comment the blocks with CMP-01.
  </action>
  <verify>
    <automated>make debug && ./build/debug/test/unittest test/sql/tabfm_scoring.test && make test_debug</automated>
    <fails_when>the CMP-01 block returns the wrong model rows / wrong golden values, the identifier-safety block errors, or any previously passing test in the full suite regresses (sqllogictest/Catch2 FAILED); SUCCESS = tabfm_scoring.test passes AND make test_debug reports the full suite green.</fails_when>
  </verify>
  <done>tabfm_compare_models / anofox_tabfm_compare_models compares tabpfn_v2 (fixture) distribution scores vs tabfm-v1 point metrics on the user's table with safe identifier quoting (CMP-01); the full test suite passes.</done>
  <reversibility rating="reversible">Additive macro in the macro-owned file + tests.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL user → PSR aggregates | (actual, yhat_dist[, coverage]) columns; malformed distribution, out-of-range coverage, out-of-support actual |
| SQL user → tabfm_compare_models macro | target/actual identifiers interpolated into a generated SQL string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Denial of Service | ComputeLogScore | medium | mitigate | Clip p to max(1e-10, p_i); guard w_i<1e-300 and out-of-support y → -log(epsilon); never log(0) or divide-by-zero |
| T-03-05 | Input Validation | IScoreBind coverage | medium | mitigate | Validate 0<coverage<1 at bind, throw named BinderException; coverage read from constant via EvaluateScalar |
| T-03-06 | Tampering (SQL injection) | tabfm_compare_models macro | high | mitigate | `replace(CAST(ident AS VARCHAR), '"', '""')` + quoting at every identifier interpolation (CV-04 pattern) — verified by an identifier-safety test block |
| T-03-07 | Denial of Service | interval/log-score STRUCT read | medium | mitigate | Same bounds-check (borders.size()==logits.size()+1, NULL checks) carried from Plan A CRPSUpdate |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new packages (RESEARCH audit: none) |

The one high-severity item (T-03-06) is fully mitigated by the carried-forward safe-quoting pattern and a dedicated identifier-safety test — no residual high risk.
</threat_model>

<verification>
- `make debug` builds the extended module.
- `./build/debug/test/unittest "[tabfm_scoring]"` — log-score + interval-score unit math pass.
- `./build/debug/test/unittest test/sql/tabfm_scoring.test` — PSR-02/03 golden, coverage-range, PSR-04 bind-gates, CMP-01 all pass.
- `make test_debug` — full suite green (capstone).
- `cd tools/parity && uv run python -m parity.crps_reference` — log-score/interval reference reproducible.
</verification>

<success_criteria>
- tabfm_log_score (PSR-02) and tabfm_interval_score (PSR-03) registered, golden-correct, bind-gated (PSR-04), with safe numerics (epsilon clip, coverage validation, zero-width/out-of-support guards).
- tabfm_compare_models (CMP-01) compares tabpfn_v2 fixture distribution scores vs tabfm-v1 point metrics on the user's table with safe identifier quoting.
- Full existing test suite passes.
</success_criteria>

<output>
Create `.planning/phases/03-proper-scoring-rules-cross-model-comparison/03-02-SUMMARY.md` when done.
</output>
