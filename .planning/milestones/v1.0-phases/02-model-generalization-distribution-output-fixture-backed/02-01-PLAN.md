---
phase: 02-model-generalization-distribution-output-fixture-backed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tabfm_profile_registry.cpp
  - src/include/tabfm_profile_registry.hpp
  - src/tabfm_preprocess_tabpfn_v2.cpp
  - src/tabfm_engine.cpp
  - src/tabfm_settings.cpp
  - src/anofox_tabfm_extension.cpp
  - src/include/tabfm_registration.hpp
  - CMakeLists.txt
  - test/cpp/test_tabfm_profile_registry.cpp
  - test/sql/tabfm_profile_registry.test
autonomous: true
requirements: [MGEN-01, MGEN-02]

estimate:
  tokens: 70000
  raw_tokens: 38000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "A model whose manifest names preprocessing_profile: 'tabfm_v1_minimal' loads and predicts exactly as before (backward-compat regression guard) (MGEN-01)"
    - "Loading a model whose manifest names an unregistered preprocessing_profile fails with a named, actionable error before any ORT run (MGEN-02)"
    - "The tabfm-v1 profile self-registers via a static initializer forced live at extension Load(), never stripped by the linker"
    - "Per-license acceptance options for known license families are pre-registered at Load() (scaffold prerequisite for MODL-03 in plan 04)"
  artifacts:
    - src/tabfm_profile_registry.cpp
    - src/include/tabfm_profile_registry.hpp
    - src/tabfm_preprocess_tabpfn_v2.cpp
    - test/cpp/test_tabfm_profile_registry.cpp
    - test/sql/tabfm_profile_registry.test
  key_links:
    - "tabfm_engine.cpp:674 PreprocessBatch(...) → DispatchPreprocess(resolved.manifest.preprocessing_profile, ...)"
    - "anofox_tabfm_extension.cpp LoadInternal → ForceProfileInit() (prevents static-init stripping)"
    - "CMakeLists.txt EXTENSION_SOURCES + TABFM_CPP_TEST_SOURCES carry every new .cpp this phase adds (registry, tabpfn_v2 preprocess, both new Catch2 tests) so later plans never touch CMakeLists"
---

<objective>
Establish the preprocessing-profile registry seam that every later plan rides on, proven end-to-end on the smallest real thing: the existing `tabfm-v1` predict path routed through the registry with the existing regression fixture still passing, plus a named error for unknown profiles. This is the tracer slice — it wires one path (manifest.preprocessing_profile → registry dispatch → PreprocessBatch → ORT → decode) through every layer the phase touches, and batches ALL scaffold-owned edits for the whole phase so plans 02–04 never collide on shared files.

Purpose: Decouple model families from the hardcoded `PreprocessBatch` call (MGEN-01/02) and land the scaffold wiring (CMakeLists source + test lists, extension Load force-init, registration decl, pre-registered per-license options) once, up front.
Output: New registry module, a compiling tabpfn_v2 preprocess stub (filled by plan 03), engine dispatch, batched scaffold edits, registry unit + SQL tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-model-generalization-distribution-output-fixture-backed/02-RESEARCH.md
@.planning/phases/02-model-generalization-distribution-output-fixture-backed/02-PATTERNS.md
@CLAUDE.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end registry dispatch — tabfm-v1 predict routed through the registry (one path only)</name>
  <files>src/include/tabfm_profile_registry.hpp, src/tabfm_profile_registry.cpp, src/tabfm_preprocess_tabpfn_v2.cpp, src/tabfm_engine.cpp, src/anofox_tabfm_extension.cpp, src/include/tabfm_registration.hpp, CMakeLists.txt</files>
  <read_first>
    - src/include/tabfm_preprocess.hpp (PreprocessedBatch, PreprocessColumnSpec, PreprocessTask, kPreprocessProfileId = "tabfm_v1_minimal", PreprocessBatch signature)
    - src/tabfm_engine.cpp lines 670-720 (the PreprocessBatch call at 674 and the resolved.manifest access)
    - src/tabfm_engine.cpp lines 781-783 (GetPredictEngine function-local-static pattern to mirror for GetRegistry)
    - src/tabfm_ort_engine.cpp lines 491-504 (dispatch-by-name + InvalidInputException actionable-text shape)
    - src/anofox_tabfm_extension.cpp (LoadInternal registration call order)
    - src/include/tabfm_registration.hpp (existing Register* declarations)
    - CMakeLists.txt lines 42-63 (EXTENSION_SOURCES) and lines 121-133 (TABFM_CPP_TEST_SOURCES)
    - .planning/phases/02-model-generalization-distribution-output-fixture-backed/02-PATTERNS.md "Pattern Assignments" for the registry + engine sections
  </read_first>
  <action>
Create the registry module and route the existing tabfm-v1 path through it. Per MGEN-01/02 and PATTERNS §"src/tabfm_profile_registry.cpp".

In `src/include/tabfm_profile_registry.hpp` (namespace duckdb::anofox, #pragma once): declare `using PreprocessFn = PreprocessedBatch(*)(const ColumnDataCollection &, const vector<PreprocessColumnSpec> &, PreprocessTask);`, a `struct ProfileRegistration { ProfileRegistration(const string &profile_id, PreprocessFn fn); };`, `PreprocessedBatch DispatchPreprocess(const string &profile_id, const ColumnDataCollection &data, const vector<PreprocessColumnSpec> &columns, PreprocessTask task);`, and `void ForceProfileInit();`. Document the ownership contract in a file-level comment naming MGEN-01/02.

In `src/tabfm_profile_registry.cpp`: implement `GetRegistry()` as a function-local-static `unordered_map<string, PreprocessFn>` inside an anonymous namespace (mirror GetPredictEngine at tabfm_engine.cpp:781). `ProfileRegistration` ctor inserts into the registry. `DispatchPreprocess` looks up the key and on miss throws `InvalidInputException` naming the fix — use the profile id and task name in the message and end with `Run: SELECT * FROM tabfm_models();` (SQL-API §5). Self-register the tabfm-v1 profile with `static const ProfileRegistration kTabFMV1Reg(kPreprocessProfileId, PreprocessBatch);` (reference the constant, not a literal). Implement `ForceProfileInit()` as an empty-bodied function whose mere existence gives LoadInternal a symbol to call, forcing this TU's static initializers to link (RESEARCH Pitfall 2). Also declare `void ForceTabPFNV2ProfileInit();` (defined in the stub file) and call it from `ForceProfileInit()` so the tabpfn_v2 profile's TU is linked too.

Create `src/tabfm_preprocess_tabpfn_v2.cpp` as a COMPILING STUB that plan 03 fills: define `PreprocessedBatch TabPFNV2PreprocessBatch(const ColumnDataCollection &, const vector<PreprocessColumnSpec> &, PreprocessTask)` that throws `NotImplementedException("tabfm: tabpfn_v2 preprocessing profile is not yet implemented (fixture arrives in plan 02-03)")` for now, self-register it with `static const ProfileRegistration kTabPFNV2Reg("tabpfn_v2", TabPFNV2PreprocessBatch);`, and define `void ForceTabPFNV2ProfileInit() {}`. This lets plan 03 fill only the body + fixture without touching CMakeLists.

Engine dispatch: at `src/tabfm_engine.cpp:674` replace `auto batch = PreprocessBatch(collection, columns, pp_task);` with `auto batch = DispatchPreprocess(resolved.manifest.preprocessing_profile, collection, columns, pp_task);`. Note the `resolved` variable is created at line ~707 (`ResolveModel`) AFTER line 674 — move the dispatch call to after `resolved` is available, or resolve the manifest's preprocessing_profile before preprocessing; keep the tensor-materialization order intact. Include `tabfm_profile_registry.hpp`. Do NOT delete PreprocessBatch (the registry calls it via function pointer).

Scaffold batch (CLAUDE.md rule #2 — coordinated, done once here for the whole phase):
- `src/include/tabfm_registration.hpp`: add `void ForceProfileInit();` declaration.
- `src/anofox_tabfm_extension.cpp`: in LoadInternal call `ForceProfileInit();` (before any predict registration).
- `CMakeLists.txt`: add `src/tabfm_profile_registry.cpp` and `src/tabfm_preprocess_tabpfn_v2.cpp` to EXTENSION_SOURCES; add `test/cpp/test_tabfm_profile_registry.cpp` AND `test/cpp/test_tabfm_distribution_decode.cpp` to TABFM_CPP_TEST_SOURCES now (plan 02 fills the decode test, plan 01 creates it as a stub in Task 2 note below — see Task 2). This is the ONLY plan that edits CMakeLists this phase.

Because CMakeLists references `test/cpp/test_tabfm_distribution_decode.cpp` (owned by plan 02) and `test/cpp/test_tabfm_profile_registry.cpp` (Task 2), create a minimal placeholder `test/cpp/test_tabfm_distribution_decode.cpp` containing only `#include "catch.hpp"` and one `TEST_CASE("distribution decode placeholder", "[tabfm][.]")` marked hidden (`[.]`) so the build links; plan 02 replaces its body.
  </action>
  <verify>
    <automated>make debug</automated>
    <fails_when>Compilation/link fails — e.g. undefined reference to DispatchPreprocess/ForceProfileInit, or CMake cannot find a listed source file.</fails_when>
  </verify>
  <verify>
    <automated>./build/debug/test/unittest test/sql/tabfm_regress.test</automated>
    <fails_when>The existing regression fixture predict regresses — any tabfm_regress row NULL/non-finite, or the two-call determinism check reports a mismatch, proving the registry-routed tabfm-v1 path broke backward compat.</fails_when>
  </verify>
  <done>make debug succeeds; tabfm_regress.test (existing tabfm-v1 path, now routed through DispatchPreprocess) passes unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registry unit + SQL error-path tests (unknown profile, tabfm-v1 dispatch)</name>
  <files>test/cpp/test_tabfm_profile_registry.cpp, test/sql/tabfm_profile_registry.test</files>
  <read_first>
    - test/cpp/test_tabfm_ort_engine.cpp (Catch2 TU conventions: #include "catch.hpp", TEST_CASE tags "[tabfm]")
    - test/sql/tabfm_regress.test lines 1-20 (require/LOAD/SET anofox_tabfm_model_manifest header block)
    - test/fixtures/manifest.json (a manifest to copy for the bad-profile case)
    - src/include/tabfm_profile_registry.hpp (DispatchPreprocess signature under test)
  </read_first>
  <behavior>
    - Test 1 (Catch2): DispatchPreprocess with an unregistered profile id (e.g. "no_such_profile") throws; the thrown message contains the profile id and the SELECT hint. Assert via REQUIRE_THROWS_WITH / Catch2 matcher on the substring, not the literal full sentence.
    - Test 2 (Catch2): DispatchPreprocess with the registered tabfm-v1 id returns a PreprocessedBatch with the expected T/train_size for a tiny hand-built ColumnDataCollection (mirror the collection-building helper used in test_tabfm_ort_engine.cpp / test_tabfm_preprocess.cpp).
    - Test 3 (SQL): a manifest whose preprocessing_profile is an unknown string causes tabfm_regress/tabfm_classify to fail with `statement error` matching the actionable substring (the profile field / SELECT hint). Use a fixtures copy or a temp manifest under __TEST_DIR__ pointing at the existing weight-free graph but with a bad profile string.
  </behavior>
  <action>
Write the failing tests FIRST (red-green TDD, CLAUDE.md rule #1), then confirm green against Task 1's implementation.

`test/cpp/test_tabfm_profile_registry.cpp`: file-level comment `// MGEN-01, MGEN-02`. Include "catch.hpp". Build a minimal ColumnDataCollection following the helper pattern in the existing preprocess/ort_engine Catch2 TUs. Assert the unknown-profile throw carries the profile id substring and the `tabfm_models()` hint substring — match SUBSTRINGS, never the full sentence (comment-text discipline).

`test/sql/tabfm_profile_registry.test`: header per tabfm_regress.test. Write a bad-profile manifest into `__TEST_DIR__` (COPY the existing test/fixtures/manifest.json contents but set `"preprocessing_profile": "no_such_profile"`), `SET anofox_tabfm_model_manifest` to it, then `statement error` on a small tabfm_classify/tabfm_regress call, matching the actionable substring only. Also add a positive case: SET back to the real fixture manifest and assert the tabfm-v1 predict still returns rows (dispatch works).

Do NOT edit CMakeLists here — Task 1 already added both new Catch2 test sources.
  </action>
  <verify>
    <automated>./build/debug/test/unittest "[tabfm]" 2>&1 | grep -v '^#' | grep -q 'profile' && ./build/debug/test/unittest test/cpp/test_tabfm_profile_registry.cpp</automated>
    <fails_when>The registry Catch2 assertions fail — unknown-profile throw missing the profile id or the SELECT hint, or the tabfm-v1 dispatch returns an empty/incorrect batch.</fails_when>
  </verify>
  <verify>
    <automated>./build/debug/test/unittest test/sql/tabfm_profile_registry.test</automated>
    <fails_when>The SQL error-path test fails — the unknown-profile manifest does not raise the named error, or the positive tabfm-v1 case returns zero rows.</fails_when>
  </verify>
  <done>Registry Catch2 tests and tabfm_profile_registry.test pass; unknown profile raises the named error; tabfm-v1 dispatch confirmed.</done>
</task>

<task type="auto">
  <name>Task 3: Pre-register per-license acceptance options at Load() (scaffold prerequisite for MODL-03)</name>
  <files>src/tabfm_settings.cpp</files>
  <read_first>
    - src/tabfm_settings.cpp lines 74-81 (existing AddExtensionOption for anofox_tabfm_accept_hf_license)
    - .planning/phases/02-model-generalization-distribution-output-fixture-backed/02-PATTERNS.md "src/tabfm_settings.cpp" section (kKnownLicenses table + pre-sanitized names)
    - .planning/phases/02-model-generalization-distribution-output-fixture-backed/02-RESEARCH.md §6 (DuckDB v1.5.4 Load-time-only constraint on AddExtensionOption)
  </read_first>
  <action>
DuckDB v1.5.4 registers extension options only at Load() (RESEARCH §6) — pre-register per-license acceptance options now so plan 04's generic gate can look them up. Keep the existing `anofox_tabfm_accept_hf_license` option untouched (backward compat). After it, add a compile-time table of known license ids with PRE-SANITIZED names (lowercase, underscores only, matching plan 04's SanitizeLicenseId output) and loop `config.AddExtensionOption(string("anofox_tabfm_accept_") + id, desc, LogicalType::BOOLEAN, Value::BOOLEAN(false))`. Seed the table with `tabfm_non_commercial_v1_0` and `tabpfn_v2_cc_by_nc_4_0` per PATTERNS. Do NOT add the fixture license (`fixture-mit` is ungated — IsGated() returns false). This task only registers options; the gate logic lands in plan 04.
  </action>
  <verify>
    <automated>make debug && ./build/debug/test/unittest test/sql/settings.test</automated>
    <fails_when>Build fails, or the settings test regresses — a duplicate/invalid option name rejected at Load(), or an existing setting assertion breaks.</fails_when>
  </verify>
  <done>make debug succeeds; the new accept-license options are registered at Load() without breaking existing settings; settings.test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest JSON → engine | Untrusted `preprocessing_profile` string crosses into the engine's dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | preprocessing_profile dispatch | high | mitigate | Registry lookup is exact-match by key; an unknown/malicious profile string fails closed with a named InvalidInputException before any ORT run or file I/O (MGEN-02). No silent fall-through. |
| T-02-02 | Denial of Service | static-init stripping leaves registry empty | medium | mitigate | ForceProfileInit() is called from LoadInternal, giving the linker a referenced symbol so the self-registering TUs are never stripped (RESEARCH Pitfall 2). |

No high-severity residuals: the untrusted profile string is fully validated at the dispatch seam before use.
</threat_model>

<verification>
- `make debug` builds the extension with the new registry module and both new test sources wired.
- `./build/debug/test/unittest test/sql/tabfm_regress.test` proves the tabfm-v1 path still works through the registry (backward-compat regression guard).
- `./build/debug/test/unittest test/cpp/test_tabfm_profile_registry.cpp` and `./build/debug/test/unittest test/sql/tabfm_profile_registry.test` prove MGEN-01/02.
</verification>

<success_criteria>
- tabfm-v1 predict routes through DispatchPreprocess and passes the existing regression fixture test unchanged (MGEN-01).
- Unknown preprocessing_profile fails with a named, actionable error before any ORT run (MGEN-02).
- ForceProfileInit() is called at Load(); both profile TUs are linked.
- Per-license acceptance options are pre-registered at Load() (prerequisite for plan 04 MODL-03).
- CMakeLists carries all new .cpp + test sources for the phase; plans 02–04 do not touch it.
</success_criteria>

<output>
Create `.planning/phases/02-model-generalization-distribution-output-fixture-backed/02-01-SUMMARY.md` when done
</output>
