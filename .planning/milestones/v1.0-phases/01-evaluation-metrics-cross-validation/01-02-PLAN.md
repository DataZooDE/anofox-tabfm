---
phase: 01-evaluation-metrics-cross-validation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/tabfm_metrics_classification.cpp
  - test/sql/tabfm_metrics_classification.test
  - test/cpp/test_tabfm_metrics.cpp
  - tools/golden/generate_metric_fixtures.py
autonomous: true
requirements: [CMET-02, CMET-03, CMET-04, CMET-05, CMET-06]
estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "tabfm_f1(actual, predicted, avg := 'macro'|'micro'|'weighted') matches scikit-learn f1_score on a 3-class fixture; omitting avg throws a named InvalidInputException (CMET-02)"
    - "tabfm_precision / tabfm_recall with required avg match sklearn (CMET-02)"
    - "tabfm_log_loss(actual, proba MAP) clips probabilities to [1e-15, 1-1e-15] — no infinities — and matches sklearn log_loss (CMET-03)"
    - "tabfm_roc_auc(actual, proba, avg := 'ovr'|'ovo') matches sklearn roc_auc_score with rank-sum tie handling; omitting avg throws a named InvalidInputException (CMET-04)"
    - "tabfm_confusion_matrix(data, actual_col, predicted_col) returns tidy (actual, predicted, count) rows (CMET-05)"
    - "tabfm_ece(actual, proba MAP) computes 10-bin Expected Calibration Error (CMET-06)"
  artifacts:
    - src/tabfm_metrics_classification.cpp
    - test/sql/tabfm_metrics_classification.test
  key_links:
    - "each aggregate registered via RegisterAggregateFunctionSetWithAlias (anofox_tabfm_* + tabfm_*)"
    - "F1/ROC-AUC bind rejects a missing avg named parameter with a fixing-message exception"
    - "log-loss/ECE read proba MAP(VARCHAR,DOUBLE) via MapValue::GetChildren + StructValue::GetChildren"
---

<objective>
As a data analyst, I want to compute the full suite of classification quality metrics —
precision/recall/F1 with an explicit averaging mode, log-loss, ROC-AUC, ECE, and a confusion
matrix — directly in SQL, so that I can characterize a classifier's quality beyond raw accuracy.

Purpose: complete the classification-metric surface (CMET-02..06), expanding from the proven
accuracy slice in plan 01-01 within the same module (file ownership: this plan owns
src/tabfm_metrics_classification.cpp after 01-01 seeded it).
Output: five more classification metrics, each golden-verified against scikit-learn, with
first-class error-path tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-evaluation-metrics-cross-validation/01-CONTEXT.md
@.planning/phases/01-evaluation-metrics-cross-validation/01-RESEARCH.md
@.planning/phases/01-evaluation-metrics-cross-validation/01-PATTERNS.md

@src/tabfm_predict_agg.cpp
@src/tabfm_devices.cpp
@src/tabfm_macros.cpp
@src/include/anofox_function_alias.hpp
@.planning/phases/01-evaluation-metrics-cross-validation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Precision / recall / F1 with a required averaging mode (CMET-02)</name>
  <files>tools/golden/generate_metric_fixtures.py, test/sql/tabfm_metrics_classification.test, src/tabfm_metrics_classification.cpp</files>
  <behavior>
    - Golden: on a 3-class fixture, tabfm_f1(actual, predicted, avg := 'macro') matches sklearn f1_score(average='macro'); same for 'micro' and 'weighted', and for precision_score/recall_score.
    - Required avg: calling tabfm_f1(actual, predicted) with no avg throws InvalidInputException naming the fix; same for precision and recall.
    - zero_division: a class absent from predictions yields precision 0.0 (sklearn zero_division=0); a class absent from actuals yields recall 0.0 — no crash.
    - NULL rows skipped.
  </behavior>
  <action>
    RED first. Add sklearn golden values for f1/precision/recall (macro/micro/weighted) on the 3-class fixture to `tools/golden/generate_metric_fixtures.py`, then add FAILING queries to `test/sql/tabfm_metrics_classification.test` including a `statement error` case for the missing-avg path expecting the message `tabfm_f1: 'avg' is required`. Run the single test, confirm RED.
    Implement per-class state `unordered_map<string,{int64 tp,int64 fp,int64 fn}>` (heap-owning → provide StateInit/StateDestroy per 01-PATTERNS.md StateDestroy pattern). Update accumulates tp/fp/fn per class, NULL-skipping. The avg mode arrives as a named parameter `avg`; validate at Bind and throw `InvalidInputException("tabfm_f1: 'avg' is required — pass avg := 'micro', 'macro', or 'weighted'")` (and the precision/recall equivalents) when absent — no silent default (D: averaging required). Finalize computes macro/micro/weighted per 01-RESEARCH.md Metric Formulas; undefined precision/recall return 0.0 (zero_division=0). Register precision/recall/f1 as anofox_tabfm_* + tabfm_* via RegisterAggregateFunctionSetWithAlias; telemetry once per bind.
    NOTE ON NAMED PARAMS: aggregates cannot take SQL named parameters directly. Implement `avg` as a required positional VARCHAR argument surfaced to users via the documented `avg := 'macro'` call form the ROADMAP success criteria pin (DuckDB routes `avg := 'macro'` to the trailing positional); if the DuckDB 1.5.4 aggregate binder does not accept the `:=` form for aggregates, fall back to a plain positional VARCHAR third argument and document the exact accepted call syntax in the SUMMARY. The invariant that MUST hold: there is NO silent default and omission throws the named error.
  </action>
  <verify>
    <automated>make debug && ./build/debug/test/unittest test/sql/tabfm_metrics_classification.test</automated>
    <fails_when>non-zero exit, or "FAILED" in output, or the missing-avg case does not raise the named error, or any f1/precision/recall golden value disagrees with sklearn</fails_when>
  </verify>
  <done>precision/recall/f1 match sklearn on the 3-class fixture for all three averaging modes; omitting avg throws the named exception; absent classes return 0.0 without crashing.</done>
  <reversibility rating="reversible">Additive metric block.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: log-loss and ECE from the per-class probability MAP (CMET-03, CMET-06)</name>
  <files>tools/golden/generate_metric_fixtures.py, test/sql/tabfm_metrics_classification.test, src/tabfm_metrics_classification.cpp</files>
  <behavior>
    - Golden log-loss: tabfm_log_loss(actual, proba) on the 3-class MAP fixture matches sklearn log_loss (tolerance 1e-6).
    - Clipping: a proba of exactly 0.0 for the true class does NOT produce infinity — it is clipped to 1e-15 (finite large loss).
    - Missing key: if the proba MAP lacks the actual class key, treat p=0 → clip to eps (no throw).
    - ECE: tabfm_ece(actual, proba) computes 10-bin equal-width Expected Calibration Error on the fixture matching a committed golden value.
    - NULL proba or NULL actual rows skipped.
  </behavior>
  <action>
    RED first. Add golden log-loss + ECE values to the generator and FAILING queries to the .test file (include the clip-to-eps finite-value case). Confirm RED.
    Implement both aggregates reading `proba MAP(VARCHAR, DOUBLE)`: iterate `MapValue::GetChildren` + `StructValue::GetChildren` in Update to find the true-class probability (01-PATTERNS.md "MAP access in Update"). log-loss state `{double sum_ll; int64 n}`: clip p to [1e-15, 1-1e-15] then accumulate -log(p); finalize sum_ll/n. ECE state `{int64 bin_n[10]; int64 bin_correct[10]; double bin_conf_sum[10]}`: for each row assign bin = floor(max_proba*10) (clamp to bin 9), track correctness (argmax class == actual) and confidence sum; finalize ECE = sum_m (bin_n[m]/N)*|acc_m - conf_m|. Document the O(N·C) MAP-lookup cost (acceptable at C≤10). Register both as anofox_tabfm_* + tabfm_* aliases; telemetry once per bind. Do NOT throw on a missing MAP key (Information-Disclosure mitigation T-01-... treat as p=0).
  </action>
  <verify>
    <automated>make debug && ./build/debug/test/unittest test/sql/tabfm_metrics_classification.test</automated>
    <fails_when>non-zero exit, or "FAILED" in output, or log-loss returns inf/nan on the zero-probability row, or the ECE golden value disagrees</fails_when>
  </verify>
  <done>log-loss matches sklearn and never returns infinity (clipped); ECE matches its golden value; missing MAP keys and NULLs handled without crashing.</done>
  <reversibility rating="reversible">Additive metric blocks.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ROC-AUC (rank-sum ties) and confusion matrix table macro (CMET-04, CMET-05)</name>
  <files>tools/golden/generate_metric_fixtures.py, test/sql/tabfm_metrics_classification.test, src/tabfm_metrics_classification.cpp, test/cpp/test_tabfm_metrics.cpp</files>
  <behavior>
    - ROC-AUC: tabfm_roc_auc(actual, proba, avg := 'ovr') matches sklearn roc_auc_score(multi_class='ovr') on a fixture WITH repeated (tied) scores, tolerance 1e-6; 'ovo' matches multi_class='ovo'.
    - Required avg: omitting avg throws InvalidInputException naming ovr/ovo.
    - Tie handling: on a fixture with equal scores across positive and negative rows, AUC equals the rank-sum (Mann-Whitney U) value, not a tie-optimistic value.
    - Confusion matrix: tabfm_confusion_matrix returns one row per observed (actual, predicted) pair with a correct count; the row set matches the fixture's grouping.
  </behavior>
  <action>
    RED first. Add sklearn AUC golden values (ovr + ovo) on a TIED-score fixture and a confusion-matrix expected row set to the generator and FAILING queries in the .test file, including the missing-avg `statement error` case (`tabfm_roc_auc: 'avg' is required`). Confirm RED.
    ROC-AUC: heap-owning state `vector<pair<double,int>>` of (score, is_positive) per class via the O(N) StateDestroy pattern; Update reads proba MAP for each class's score; Finalize sorts descending, groups equal-score ties, integrates trapezoidally (equivalently the Mann-Whitney U / average-rank form) — see 01-RESEARCH.md ROC-AUC formula + Pitfall 1. ovr averages per-class binary AUC; ovo averages all C*(C-1)/2 pair AUCs. Bind requires avg → throw `InvalidInputException("tabfm_roc_auc: 'avg' is required — pass avg := 'ovr' or 'ovo'")` when absent (same positional/`:=` handling note as Task 1). Register aggregate + alias; telemetry once per bind.
    Confusion matrix: per 01-RESEARCH.md Open Question 2, implement `tabfm_confusion_matrix` as a TABLE MACRO wrapping `SELECT <actual>, <predicted>, COUNT(*) AS count FROM query('FROM ' || data) GROUP BY 1, 2` — but this macro lives in the classification module's register function. Since a table macro belongs to macro registration, register it inside RegisterClassificationMetrics using the TableMacroFunction build pattern from 01-PATTERNS.md (BuildTableMacroFunction/BuildMacroInfo), returning tidy (actual, predicted, count). Quote both column identifiers with `replace(col, '"', '""')` double-quote wrapping when interpolating them into the GROUP BY SQL (same hardening as tabfm_macros.cpp:93). Register anofox_tabfm_confusion_matrix + tabfm_confusion_matrix alias; telemetry once per macro bind.
    Add a Catch2 case in test_tabfm_metrics.cpp for AUC tie handling through a Connection.
  </action>
  <verify>
    <automated>make debug && ./build/debug/test/unittest test/sql/tabfm_metrics_classification.test && ./build/debug/test/unittest "[tabfm][metrics]"</automated>
    <fails_when>non-zero exit, or "FAILED" in output, or AUC on the tied-score fixture disagrees with sklearn beyond 1e-6, or the missing-avg case does not raise, or confusion-matrix rows/counts differ from the expected set</fails_when>
  </verify>
  <done>ROC-AUC matches sklearn (ovr + ovo) with correct rank-sum tie handling and requires avg; confusion matrix returns correct tidy (actual, predicted, count) rows with safely-quoted identifiers.</done>
  <reversibility rating="reversible">Additive metric + macro blocks.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL query → aggregate Update | Untrusted actual/predicted/proba column values enter C++ accumulation |
| confusion-matrix macro → generated SQL | User-supplied actual_col/predicted_col identifiers interpolated into a GROUP BY string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02-01 | Tampering | confusion_matrix column-identifier interpolation | medium | mitigate | Double-quote + escape both column identifiers with replace(col,'"','""') (same as tabfm_macros.cpp:93) |
| T-01-02-02 | Information Disclosure | log-loss missing proba MAP key | low | mitigate | Treat missing key as p=0 → clip to eps; never throw or leak internal state |
| T-01-02-03 | Denial of Service | division by zero in F1/AUC/ECE finalize | low | mitigate | All denominators guarded; undefined precision/recall → 0.0; empty state → NULL |
| T-01-02-04 | Tampering | npm/pip/cargo installs | n/a | accept | No package installs; RESEARCH.md Package Legitimacy Audit records zero packages |

No high-severity threats (ASVS L1). The medium identifier-interpolation threat is fully mitigated by the same double-quote hardening used elsewhere in the codebase.
</threat_model>

<verification>
- All classification golden queries pass against committed sklearn values.
- Missing-avg error paths raise the named exceptions (F1 + ROC-AUC).
- log-loss never returns inf/nan (clipping verified).
- confusion_matrix returns correct tidy rows; identifiers safely quoted.
- Each user-facing function has exactly one telemetry call at bind.
</verification>

<success_criteria>
CMET-02..06 all implemented, golden-verified against scikit-learn, with first-class error-path tests; averaging mode is required with no silent default.
</success_criteria>

<output>
Create `.planning/phases/01-evaluation-metrics-cross-validation/01-02-SUMMARY.md` when done.
</output>
