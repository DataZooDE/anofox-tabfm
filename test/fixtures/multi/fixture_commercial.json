{
  "schema_version": 2,
  "id": "fixture-commercial",
  "display_name": "Fixture (commercial, schema v2)",
  "family": "icl-transformer",
  "license": {
    "id": "apache-2.0",
    "commercial": true,
    "redistributable": true,
    "gate_setting": null
  },
  "preprocessing_profile": "tabfm_v1_minimal",
  "weights": {
    "classification": {
      "files": [
        { "path": "../model.safetensors", "bytes": 103324 }
      ]
    }
  },
  "graph": {
    "classification": "../graph_fixture.onnx",
    "tensor_map": "../tensor_map_fixture.json"
  },
  "capabilities": ["classify"],
  "tensor_contract": {
    "inputs": {
      "features":   { "name": "x",          "dtype": "f32" },
      "labels":     { "name": "y",          "dtype": "f32" },
      "train_size": { "name": "train_size", "dtype": "i64" },
      "cat_mask":   { "name": "cat_mask",   "dtype": "bool" },
      "n_features": { "name": "d",          "dtype": "i64" }
    },
    "outputs": {
      "logits": { "name": "logits", "dtype": "f32" }
    }
  },
  "size_regime": { "max_rows": 5000, "max_features": 100, "max_classes": 10 },
  "compute": { "cpu": "f32" },
  "_note": "A second CI fixture model — v2 schema, commercial (ungated) license — that REUSES the committed classification fixture graph/weights via ../ paths. Zero new artifacts, no Mitra bytes; proves the registry runs a v2 manifest end-to-end (FR-5.1 / M4)."
}
