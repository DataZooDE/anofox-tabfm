{
  "schema_version": 2,
  "id": "fixture-badcontract",
  "display_name": "Fixture (lying tensor_contract)",
  "family": "icl-transformer",
  "license": {
    "id": "apache-2.0",
    "commercial": true,
    "redistributable": true,
    "gate_setting": null
  },
  "preprocessing_profile": "tabfm_v1_minimal",
  "weights": {
    "classification": {
      "files": [
        { "path": "../model.safetensors", "bytes": 103324 }
      ]
    }
  },
  "graph": {
    "classification": "../graph_fixture.onnx",
    "tensor_map": "../tensor_map_fixture.json"
  },
  "capabilities": ["classify"],
  "tensor_contract": {
    "inputs": {
      "features": { "name": "not_a_real_input", "dtype": "f32" }
    },
    "outputs": {
      "logits": { "name": "logits", "dtype": "f32" }
    }
  },
  "size_regime": { "max_rows": 5000, "max_features": 100, "max_classes": 10 },
  "compute": { "cpu": "f32" },
  "_note": "Negative P4 fixture: the tensor_contract declares input 'not_a_real_input' which the committed graph does NOT have. Loading a session for this model must fail fast with an actionable 'manifest does not match its graph' error. Reuses the classification fixture artifacts via ../ paths — no new bytes."
}
