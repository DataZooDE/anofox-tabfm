//===----------------------------------------------------------------------===//
// test_tabfm_distribution_decode.cpp — placeholder (plan 02-02 fills this body)
//
// This file is a build placeholder committed by plan 02-01 so CMakeLists.txt
// can list it in TABFM_CPP_TEST_SOURCES without a compile error.
// Plan 02-02 replaces this body with the golden-decode Catch2 tests for
// RDIST-01/02 (distribution decode: logits + borders → mean/quantiles).
//
// The single hidden test case ensures the file compiles and links without
// running in the default test suite.
//===----------------------------------------------------------------------===//

#include "catch.hpp"

// Hidden test case ([.] suffix): not run by default; plan 02-02 replaces this.
TEST_CASE("distribution decode placeholder", "[tabfm][.]") {
	REQUIRE(true);
}
