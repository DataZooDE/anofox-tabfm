// Catch2 tests for tabfm_safetensors — owned by its workstream (see CLAUDE.md).
// Pre-registered in CMakeLists.txt by the scaffold so workstreams never edit
// shared files; replace the placeholder case with real tests.

#include "catch.hpp"

TEST_CASE("tabfm_safetensors: placeholder until workstream lands", "[tabfm][safetensors]") {
	SUCCEED();
}
