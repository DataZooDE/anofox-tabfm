// Catch2 tests for tabfm_ort_engine — owned by its workstream (see CLAUDE.md).
// Pre-registered in CMakeLists.txt by the scaffold so workstreams never edit
// shared files; replace the placeholder case with real tests.

#include "catch.hpp"

TEST_CASE("tabfm_ort_engine: placeholder until workstream lands", "[tabfm][ort_engine]") {
	SUCCEED();
}
