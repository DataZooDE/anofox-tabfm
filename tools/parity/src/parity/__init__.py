"""anofox-tabfm parity — MODL-04 fixture contract validation.

Validates that the committed tabpfn_v2 K=16 fixture ONNX graph emits
both named distribution tensors with the confirmed contract shapes and
properties, before the C++ decoder is trusted.
"""
